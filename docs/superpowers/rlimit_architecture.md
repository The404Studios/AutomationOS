# Resource Limits Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Space                               │
├─────────────────────────────────────────────────────────────────┤
│  Application                                                      │
│    ├── malloc()  ──────────────┐                                │
│    ├── open()    ──────────────┼───────────┐                    │
│    ├── send()    ──────────────┼───────────┼──────────┐         │
│    └── fork()    ──────────────┼───────────┼──────────┼───┐     │
└────────────────────────────────┼───────────┼──────────┼───┼─────┘
                                 │           │          │   │
                   System Call Interface                │   │
                                 │           │          │   │
┌────────────────────────────────┼───────────┼──────────┼───┼─────┐
│                    Kernel Space │           │          │   │     │
├────────────────────────────────┼───────────┼──────────┼───┼─────┤
│                                ▼           ▼          ▼   ▼     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            Syscall Dispatch Layer                        │   │
│  │  sys_setrlimit() | sys_getrlimit() | sys_getrusage()    │   │
│  └─────────────┬────────────────────────────────────────────┘   │
│                │                                                 │
│  ┌─────────────▼─────────────────────────────────────────────┐  │
│  │         Resource Limit Framework (rlimit.c)               │  │
│  │  ┌──────────────────────────────────────────────────┐    │  │
│  │  │        rlimit_container_t                        │    │  │
│  │  │  ┌────────────────────────────────────────┐     │    │  │
│  │  │  │  limits[RLIMIT_MAX]                    │     │    │  │
│  │  │  │    - RLIMIT_CPU    : {soft, hard}      │     │    │  │
│  │  │  │    - RLIMIT_MEMORY : {soft, hard}      │     │    │  │
│  │  │  │    - RLIMIT_NOFILE : {soft, hard}      │     │    │  │
│  │  │  │    - RLIMIT_NET_TX : {soft, hard}      │     │    │  │
│  │  │  └────────────────────────────────────────┘     │    │  │
│  │  │  ┌────────────────────────────────────────┐     │    │  │
│  │  │  │  rusage_t (usage statistics)           │     │    │  │
│  │  │  │    - cpu_time                          │     │    │  │
│  │  │  │    - memory_current, memory_peak       │     │    │  │
│  │  │  │    - fd_count                          │     │    │  │
│  │  │  │    - net_rx_bytes, net_tx_bytes        │     │    │  │
│  │  │  └────────────────────────────────────────┘     │    │  │
│  │  │  ┌────────────────────────────────────────┐     │    │  │
│  │  │  │  cpu_quota_t                           │     │    │  │
│  │  │  │    - shares (proportional weight)      │     │    │  │
│  │  │  │    - quota_us, period_us               │     │    │  │
│  │  │  │    - used_us, throttled                │     │    │  │
│  │  │  └────────────────────────────────────────┘     │    │  │
│  │  │  ┌────────────────────────────────────────┐     │    │  │
│  │  │  │  token_bucket_t (x4)                   │     │    │  │
│  │  │  │    - net_rx_bucket                     │     │    │  │
│  │  │  │    - net_tx_bucket                     │     │    │  │
│  │  │  │    - disk_read_bucket                  │     │    │  │
│  │  │  │    - disk_write_bucket                 │     │    │  │
│  │  │  └────────────────────────────────────────┘     │    │  │
│  │  └──────────────────────────────────────────────────┘    │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─────────────────┬──────────────┬──────────────┬──────────┐  │
│  │   CPU Module    │ Memory Module│   FD Module  │ Net Module│ │
│  ├─────────────────┼──────────────┼──────────────┼───────────┤ │
│  │ cpu.c           │ memory.c     │ fd.c         │ network.c │ │
│  │                 │              │              │           │ │
│  │ • check_cpu()   │ • check_mem()│ • check_fd() │• check_tx()│ │
│  │ • account_cpu() │ • account_   │ • account_   │• account_ │ │
│  │ • set_shares()  │   alloc()    │   fd_open()  │   tx()    │ │
│  │ • set_quota()   │ • account_   │ • account_   │• token_   │ │
│  │ • quota_        │   free()     │   fd_close() │  bucket() │ │
│  │   exceeded()    │ • oom_kill() │ • get_       │• refill() │ │
│  │ • refill()      │ • pressure() │   pressure() │           │ │
│  └─────────┬───────┴──────┬───────┴──────┬───────┴───────┬───┘ │
│            │              │              │               │     │
│  ┌─────────▼──────────────▼──────────────▼───────────────▼───┐ │
│  │              Enforcement Points                             │ │
│  └─────┬──────────┬──────────┬──────────┬──────────┬──────┬──┘ │
│        │          │          │          │          │      │    │
│  ┌─────▼───┐ ┌────▼────┐ ┌──▼────┐ ┌───▼────┐ ┌──▼───┐ │     │
│  │Scheduler│ │ Memory  │ │  VFS  │ │ Network│ │ Fork │ │...  │
│  │         │ │ Allocator│ │       │ │ Stack  │ │      │ │     │
│  │schedule()│ │kmalloc()│ │open() │ │send()  │ │fork()│ │     │
│  │         │ │kfree()  │ │close()│ │recv()  │ │      │ │     │
│  └─────────┘ └─────────┘ └───────┘ └────────┘ └──────┘ │     │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     Process Table                         │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │  │
│  │  │ process_t  │  │ process_t  │  │ process_t  │   ...   │  │
│  │  │  pid: 1    │  │  pid: 2    │  │  pid: 3    │         │  │
│  │  │  rlimits ──┼──│  rlimits ──┼──│  rlimits ──┼──┐      │  │
│  │  └────────────┘  └────────────┘  └────────────┘  │      │  │
│  │       │                 │                 │       │      │  │
│  │       ▼                 ▼                 ▼       ▼      │  │
│  │  [container]       [container]       [container]        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

## Component Interactions

### 1. Process Creation Flow

```
process_create()
    │
    ├─► rlimit_create_container()
    │       └─► Initialize default limits
    │
    └─► OR rlimit_inherit_container(parent)
            └─► Copy limits, reset usage
```

### 2. Resource Allocation Flow (Memory Example)

```
kmalloc(size)
    │
    ├─► process_get_current()
    │       └─► Get current process
    │
    ├─► rlimit_check_memory(rl, size)
    │       ├─► Check soft limit
    │       ├─► Check hard limit
    │       └─► Return true/false
    │
    ├─► [IF LIMIT EXCEEDED]
    │       └─► rlimit_oom_kill(proc)
    │               └─► Terminate process
    │
    ├─► internal_alloc(size)
    │       └─► Actual allocation
    │
    └─► rlimit_account_memory_alloc(rl, size)
            ├─► Update memory_current
            └─► Update memory_peak
```

### 3. CPU Scheduling Flow

```
schedule() [Timer Interrupt]
    │
    ├─► rlimit_cpu_tick(rl, ticks)
    │       ├─► Convert ticks to ms
    │       ├─► rlimit_check_cpu(rl, ms)
    │       │       ├─► Check hard limit
    │       │       └─► Signal soft limit
    │       ├─► rlimit_account_cpu(rl, ms)
    │       │       └─► Update cpu_time, used_us
    │       └─► rlimit_cpu_quota_exceeded(rl)
    │               ├─► Check quota vs period
    │               └─► Set throttled flag
    │
    ├─► [IF QUOTA EXCEEDED]
    │       └─► Set process to BLOCKED
    │
    ├─► rlimit_can_schedule(rl)
    │       └─► Return true/false
    │
    └─► scheduler_pick_next()
            └─► Select next runnable process
```

### 4. Network Bandwidth Flow (Token Bucket)

```
socket_send(sockfd, buf, len)
    │
    ├─► rlimit_check_network_tx(rl, len)
    │       │
    │       ├─► token_bucket_refill(bucket, current_time)
    │       │       ├─► elapsed = current - last_update
    │       │       ├─► tokens_add = rate * elapsed / 1000
    │       │       └─► tokens = min(tokens + add, capacity)
    │       │
    │       └─► token_bucket_consume(bucket, len)
    │               ├─► IF tokens >= len
    │               │       ├─► tokens -= len
    │               │       └─► Return true
    │               └─► ELSE Return false
    │
    ├─► [IF RATE LIMITED]
    │       └─► Return -EAGAIN
    │
    ├─► net_send(sockfd, buf, len)
    │       └─► Actual network transmission
    │
    └─► rlimit_account_network_tx(rl, sent)
            └─► Update net_tx_bytes
```

## Data Structures

### Token Bucket State Machine

```
State: FULL (tokens = capacity)
    │
    ├─► consume(100) ─────► PARTIAL (tokens = capacity - 100)
    │                            │
    │                            ├─► consume(50) ───► PARTIAL (tokens -= 50)
    │                            │
    │                            └─► consume(1000) ─► EMPTY (tokens = 0)
    │                                                      │
    │                                                      ├─► consume() ─► DENIED
    │                                                      │
    ▼                                                      ▼
Time passes (1 second)                              Time passes
    │                                                      │
    └─► refill() ─────────────────────────────────────────┘
            │
            └─► tokens += rate
                    │
                    └─► IF tokens > capacity: tokens = capacity
```

### CPU Quota State Machine

```
New Period Starts
    │
    ├─► used_us = 0
    ├─► throttled = false
    └─► period_start = current_time
            │
            ▼
    ┌───────────────────┐
    │   RUNNING         │
    │ (used < quota)    │
    └─────┬─────────────┘
          │
          ├─► account_cpu(ms)
          │       └─► used_us += (ms * 1000)
          │
          ├─► [IF used_us >= quota_us]
          │       └─► State = THROTTLED
          │
          ▼
    ┌───────────────────┐
    │   THROTTLED       │
    │ (used >= quota)   │
    └─────┬─────────────┘
          │
          └─► [Process blocked until next period]
                  │
                  ▼
          ┌───────────────────┐
          │ Period Expired    │
          │ (time >= period)  │
          └─────┬─────────────┘
                │
                └─► refill() ──► Back to RUNNING
```

## Memory Layout

### Per-Process Memory Overhead

```
process_t structure:
┌────────────────────────┐
│ pid, state, context    │  ~100 bytes
├────────────────────────┤
│ rlimits ──────────────►│
│                        │
│ [Other fields...]      │
└────────────────────────┘
                          │
                          ▼
        rlimit_container_t:
        ┌─────────────────────────────┐
        │ limits[10]                  │  10 × 16 = 160 bytes
        ├─────────────────────────────┤
        │ usage (rusage_t)            │  ~100 bytes
        ├─────────────────────────────┤
        │ cpu_quota_t                 │  ~40 bytes
        ├─────────────────────────────┤
        │ token_bucket_t × 4          │  4 × 32 = 128 bytes
        ├─────────────────────────────┤
        │ soft_limit_signaled[10]     │  10 bytes
        └─────────────────────────────┘
        Total: ~438 bytes per process
```

## Performance Characteristics

### Operation Costs

```
Fast Path (No Limits):
┌──────────────┐
│ kmalloc()    │  Cost: +0 ns (no overhead)
└──────────────┘

With Limits:
┌──────────────┐
│ kmalloc()    │
├──────────────┤
│ check_limit  │  Cost: ~50 ns (comparison)
├──────────────┤
│ alloc        │  Cost: N (original)
├──────────────┤
│ account      │  Cost: ~50 ns (increment)
└──────────────┘
Total overhead: ~100 ns (~1% for typical alloc)

Token Bucket (Network):
┌──────────────┐
│ send()       │
├──────────────┤
│ refill       │  Cost: ~100 ns (arithmetic)
├──────────────┤
│ consume      │  Cost: ~50 ns (comparison)
├──────────────┤
│ net_send     │  Cost: N (original)
├──────────────┤
│ account      │  Cost: ~50 ns (increment)
└──────────────┘
Total overhead: ~200 ns (~0.1% for typical packet)
```

## Integration Points Summary

```
┌─────────────────────────────────────────────────────────┐
│                  Integration Layer                       │
├──────────────┬──────────────┬──────────────┬────────────┤
│  Scheduler   │   Memory     │     VFS      │  Network   │
│              │              │              │            │
│ schedule()   │ kmalloc()    │ sys_open()   │ send()     │
│   ├─ tick    │   ├─ check   │   ├─ check   │   ├─ check │
│   ├─ check   │   ├─ alloc   │   ├─ open    │   ├─ send  │
│   └─ pick    │   └─ account │   └─ account │   └─ acct  │
│              │              │              │            │
│ CREATE:      │ kfree()      │ sys_close()  │ recv()     │
│   └─ create/ │   ├─ free    │   └─ account │   └─ acct  │
│      inherit │   └─ account │              │            │
│              │              │              │            │
│ DESTROY:     │              │              │            │
│   └─ destroy │              │              │            │
└──────────────┴──────────────┴──────────────┴────────────┘
```

---

**Key Insights**:
1. Central rlimit_container_t per process
2. Modular enforcement (CPU, mem, FD, net separate)
3. Check-then-account pattern everywhere
4. Token bucket for bandwidth, quota for CPU
5. Low overhead (~1%) in common case
