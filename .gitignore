# Build artifacts
build/
iso/
*.o
*.elf
*.bin
*.iso

# Framebuffer captures / one-off screenshots (curated shots live in screenshots/)
*.ppm
/boot_shot.png
/boot_fade.png
/boot_settled.png
/desktop_final.png

# Stray in-tree compiled binaries (build outputs that landed in source dirs)
userspace/compositor/compositor
userspace/shell/shell
userspace/wm/wm
userspace/test_ring3/init_bare
userspace/test_ring3/terminal
userspace/test_ring3/test_ring3

# Claude Code workspace (never publish)
.claude/

# Build logs & loose scratch files (root-level dev cruft)
*.log
/kerr*.txt
/kout*.txt
/uerr*.txt
/uout*.txt
/t.c
/tmp*.c
/p256_*.c
*_SUMMARY.txt
*_DELIVERABLES.txt
*_VERIFICATION.txt
*_IMPLEMENTATION.txt

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Cryptographic keys (NEVER COMMIT!)
keys/
*.pem
*.key
*.crt
*.p12
*.pfx

# Session / agent status artifacts (not part of the project source)
# Anchored to repo root so it only ignores top-level AGENT scratch docs, NOT
# nested records like docs/dev-memory/bricks/AGENT-RPC-0.md.
/AGENT*.md
*_REPORT.md
*_SUMMARY.md
*_DELIVER*.md
*_DELIVERY.md
*_CHECKLIST.md
*_COMPLETE.md
*_VALIDATION*.md
ACCESSIBILITY_*.md
AUTOFS_*.md
ANIMATION_*.md
.task*tracker*
.task*.md
.phase*tracker*

# --- agent-stabilize-0 cleanup: dev scratch (deleted from tree, keep ignored) ---
*.bak
_diff_*.txt
/screenshots/

# Root-level ALL-CAPS session notes (one-off design drafts, not project docs).
# Anchored to root so nested docs/dev-memory/bricks/*.md stay tracked.
/IDE_*.md
/PANIC_*.md
/TCP_*.md
/HDA_*.md
/GOTO_*.md
/MOUSE_*.md
/KEYBOARD_*.md
/LEAK_*.md
/IPI_*.md
/RAPID_*.md
/UX_*.md
/IMPLEMENTATION_*.md
/PMM_*.md

# build_test one-off run outputs (underscore-prefixed). NOTE: the 3 tracked
# harnesses _dsplit_runner.sh / _dsplit_verdict.sh / _ti_soak_runner.sh are .sh
# and stay tracked; we only ignore loose output artifacts here.
build_test/_*.out
build_test/_*.tmp
build_test/_*.patch

# Python bytecode cache
__pycache__/
*.pyc
