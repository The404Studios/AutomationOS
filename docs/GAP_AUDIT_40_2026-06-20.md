# AutomationOS — 40-Agent Gap Audit (2026-06-20)
Generated by a 40-agent parallel read-only audit. **238 findings** (P0=12 P1=77 P2=100 P3=49) across 40 subsystem slices.
> Agent-reported; ~30%% may be false-positive or mischaracterized. VERIFY against HEAD before acting. `fillable_now` = agent's guess that a localized edit fixes it; treat as a hint, not a guarantee.

## ramfs (6)
- **[P0 c=1.00]** Page cache is known to corrupt file data — reads/writes bypass it entirely — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:1458-1460, 1504-1505`
  - Both ramfs_read() and ramfs_write() contain explicit comments that page_cache_read/page_cache_write corrupt data and slow boot. The code intentionally uses direct memcpy paths, completely bypassing the cache layer. This is a hard workaround, not a fix: the page cache is registered and initialized, but disabled for correctness. Writes via page_cache_write() would be lost on unmount or process exit.
  - *fix:* Debug and fix the page_cache_read/page_cache_write implementations (in page_cache.c). The cache_entry_alloc bug (lines 46-49) was already fixed; investigate offset math, dirty-page flushing, and partial-page handling. Once fixed, re-enable page_cache_write in ramfs_write.
- **[P1 c=0.95 fillable]** Relative symlinks not supported — hard abort on traversal — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:733-741`
  - When vfs_path_lookup encounters a relative symlink (target doesn't start with '/'), it immediately returns NULL with no error code differentiation. This is documented as a known limitation ('for simplicity, we'll just fail') but causes path lookups to fail silently rather than falling back or reporting ELOOP. A file containing a relative symlink becomes unreachable even if its target exists.
  - *fix:* Implement relative symlink resolution by building the full path from the current directory + symlink target, then recursing into vfs_path_lookup_rec(). The infrastructure (rec_depth, target buffer, heap allocation) is already in place.
- **[P1 c=0.90]** No fsync/sync actually persist ramfs data — all in-memory without WAL or durability — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:1274-1305`
  - vfs_fsync() calls page_cache_flush_inode() and vfs_sync() calls page_cache_flush_all(), but since page_cache_write is disabled (see P0 finding), dirty data in ramfs never enters the cache in the first place. More critically, ramfs has no persistent storage backend — all data is heap-allocated and lost on shutdown. There is no write-ahead log, journal, or checkpoint mechanism. Applications relying 
  - *fix:* Document that ramfs is volatile (initrd-backed read-only + tmpfs semantics, no durability). If durability is needed, implement a proper journal or redirect writes to a persistent filesystem (ext2/FAT32). For safety, have ramfs open() fail with EROFS unless the application explicitly accepts volatility.
- **[P1 c=0.80 fillable]** ramfs_write() silently truncates CoW copies > 256 MiB, corrupting large files — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:1520-1525`
  - RAMFS_MAX_FILE_SIZE is a hard cap of 256 MiB per file. When an initrd-backed file undergoes CoW (lines 1535+), if the result would exceed 256 MiB, ramfs_write returns -1. However, lines 1549-1551 contain logic that silently clamps copy = inode->size to new_capacity, which can truncate original data during the malloc/memcpy if new_capacity < inode->size. This is a safety guard, but the 256 MiB limi
  - *fix:* Either increase RAMFS_MAX_FILE_SIZE or implement tiered storage (spill to ext2 on overflow). Alternatively, document that initrd files > 128 MiB cannot be written; add a syscall to detect and warn userspace.
- **[P2 c=0.65 fillable]** Copy-on-write for initrd-backed files doesn't preserve sparse gaps correctly — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:1535-1564`
  - When ramfs_write() triggers CoW for an initrd-backed file (VFS_DATA_INITRD_BACKED), it allocates new_capacity = required * 2 (with growth headroom). After a sparse write (e.g., lseek 1000, write 10 bytes at offset 2000), memset zeros the entire new_capacity, then memcpy only copies inode->size bytes of old data. This means bytes [inode->size, file->offset) are zeroed, not left uninitialized. The c
  - *fix:* If sparse gap exists (file->offset > inode->size), zero only [inode->size, file->offset) before the write, not the entire new capacity. Or document the behavior clearly if all ramfs files are expected to be sparse-friendly.
- **[P2 c=0.60 fillable]** Directory capacity overflow not checked during ramfs_dir_add growth — double overflow silently returns -1 — `/c/Users/wilde/Desktop/Kernel/kernel/fs/vfs.c:106-109`
  - ramfs_dir_add() checks if new_cap = dir->data_capacity * 2 overflows and returns -1 if so, but this silently fails the add without logging or distinguishing between OOM and capacity overflow. A directory with 2^63 entries would trigger this (unrealistic), but the fix is incomplete: if caller doesn't handle -1 correctly, the directory remains corrupted (caller's dentry not inserted, but caller may 
  - *fix:* Log the overflow case distinctly (e.g., kprintf) so operators notice. Alternatively, cap capacity at a reasonable maximum (e.g., 2^20 entries per directory) and fail earlier with a more graceful error message.

## fat32 (6)
- **[P0 c=1.00]** fat32_mount: block device lookup always returns NULL, mount always fails — `kernel/fs/fat32.c:544-554`
  - The fat32_mount function at lines 544-554 explicitly sets block_dev=NULL with a comment 'Block device lookup not implemented yet', then immediately exits if block_dev is NULL (line 549). This means FAT32 can never be mounted; the entire filesystem is non-functional.
  - *fix:* Implement block device lookup using block_get_device(source) to retrieve the block device by name/path. Alternatively, if block_get_device doesn't exist, create a block device registry lookup function.
- **[P1 c=0.95 fillable]** fat32_detect: uninitialized buffer read before block_read call — `kernel/fs/fat32.c:489-503`
  - fat32_detect allocates a boot_sector buffer but never actually reads from the block device. It immediately checks boot_sector->signature (line 501) without calling block_read first. This reads uninitialized stack/heap memory, causing false positives (any garbage matching 0xAA55 passes) and potential kernel crashes on invalid pointers.
  - *fix:* Add block_get_device(source) call to retrieve the block device, then call block_read(dev, 0, 1, boot_sector) before checking fields. Return -1 if device lookup or read fails.
- **[P1 c=0.90 fillable]** bytes_per_cluster multiplication overflow and lack of range clamping — `kernel/fs/fat32.c:603-604`
  - bytes_per_cluster is computed as (uint32_t)(bytes_per_sector * sectors_per_cluster) with no overflow check. Malformed boot sectors (e.g., bytes_per_sector=65535, sectors_per_cluster=128) wrap to small values, breaking cluster math. Additionally, bytes_per_cluster has no range validation (standard FAT32 is 512-32KB); unbounded values cause kmalloc failures or integer overflow in subsequent division
  - *fix:* After multiplication, validate 512 <= bytes_per_cluster <= 32768. Check for overflow: if (bytes_per_sector > UINT32_MAX / sectors_per_cluster) reject. Clamp or reject invalid values before use.
- **[P1 c=0.90 fillable]** Integer overflow in fat_size_bytes and FAT allocation without overflow check — `kernel/fs/fat32.c:618-619`
  - fat_size_bytes = fat_size_32 * bytes_per_sector (both uint32_t) can overflow silently. A crafted boot sector (fat_size_32 = 0xFFFFFFFF, bytes_per_sector = 4096) wraps to a small allocation size. kmalloc then allocates insufficient memory, and the subsequent block_read at line 627 writes beyond the buffer, corrupting kernel memory.
  - *fix:* Check for overflow before multiplication: if (fat_size_32 > UINT32_MAX / bytes_per_sector) reject the filesystem. Ensure the FAT allocation size is sensible (e.g., <= 1 GB).
- **[P1 c=0.85 fillable]** OOB FAT index access: first_cluster and root_cluster not validated against total_clusters — `kernel/fs/fat32.c:365-366, 458-465`
  - Directory cluster (root_cluster from mount, first_cluster from entries) is used directly to index the FAT at line 62 (fat[cluster]) without bounds checking. A malicious directory entry with first_cluster >= total_clusters or root_cluster >= total_clusters causes OOB array access into the FAT table. fat32_get_next_cluster checks cluster < total_clusters at line 58, but the initial value (root_clust
  - *fix:* In fat32_vfs_lookup at line 465 and in fat32_mount at line 665, validate first_cluster < total_clusters and root_cluster < total_clusters before storing in private_data. Return error if out of bounds.
- **[P2 c=0.70 fillable]** fat32_vfs_write is a complete stub, no write support — `kernel/fs/fat32.c:171-174`
  - The fat32_vfs_write function unconditionally returns -1 with a comment 'read-only for now'. This is expected for a read-only implementation but should be documented at mount time (e.g., set O_RDONLY flag on inode or in mount flags) to prevent user confusion and potential data loss attempts.
  - *fix:* Either implement write support (FAT chain allocation, directory entry updates, data sector writes) or set read-only flag on VFS inodes at mount time. Document the limitation in kprintf at mount completion.

## nvme (6)
- **[P0 c=1.00 fillable]** nvme_init() never called from kernel boot sequence — `kernel/drivers/storage/nvme.c:61-89`
  - The nvme_init() function is defined and exported in drivers.h, but is never invoked from kernel.c during boot. While hda_init() and other drivers are explicitly called (kernel/kernel.c:944-945), nvme_init() is absent from the kernel initialization path, meaning NVMe controllers are never detected or initialized at boot time. The function exists only in test code (kernel/test_nvme.c) and documentat
  - *fix:* Add nvme_init() call in kernel/kernel.c's boot sequence, following the pattern used for hda_init(). Place it after PCI enumeration completes but before storage subsystem is expected to be available.
- **[P0 c=1.00]** PRP list corruption for buffers >8KB: oversimplified two-entry implementation — `kernel/drivers/storage/nvme.c:690-700`
  - nvme_build_prp_list() assumes all transfers fit in 2 PRP entries (prp1 + prp2). For buffers larger than 8KB, it naively sets prp2 = phys_addr + NVME_PAGE_SIZE. The NVMe spec requires: if a transfer spans >2 pages, prp2 must be a POINTER TO A PRP LIST (not the second page's address directly). When DMA attempts to fetch data from the incorrect address in prp2, it reads/writes to the wrong memory loc
  - *fix:* Implement proper PRP list handling: (1) if transfer <=8KB, use simple two-entry mode (prp1=page1, prp2=page2 or 0). (2) if transfer >8KB, allocate a PRP list page and populate it with the addresses of all required data pages, then set prp2 to the PRP list page address. Consider capping maximum transfer size or using SGL (Scatter-Gather List) alternative if available.
- **[P1 c=1.00 fillable]** Single-controller enumeration: pci_find_class() returns first device only — `kernel/drivers/storage/nvme.c:65-82`
  - The nvme_init() function calls pci_find_class() once, which returns only the FIRST matching NVMe device (by design in pci.c:671-681). The while loop at line 67 then sets pci_dev=NULL (line 81), breaking enumeration. Systems with multiple NVMe drives will only detect the first one. The comment acknowledges this: 'TODO: Implement PCI enumeration iteration'.
  - *fix:* Implement iterator-based PCI enumeration: either (1) add pci_find_class_next() API to return subsequent matches, or (2) iterate g_pci_devices[] directly by class/subclass/prog_if to support multiple controllers. Update nvme_init() to call this in the while loop.
- **[P1 c=1.00 fillable]** Multi-page queue allocation uses wrong free function — `kernel/drivers/storage/nvme.c:412-452`
  - nvme_setup_io_queues() allocates 4 contiguous pages for the I/O submission queue with pmm_alloc_pages(4) (line 412), but attempts to free it with pmm_free_page() on error paths (lines 417, 441, 450). According to mem.h:76-77, pmm_alloc_pages(count) must be freed with pmm_free_pages(base, count), not pmm_free_page(). This causes memory leaks on queue allocation failures and leaves incorrect PMM sta
  - *fix:* Replace all pmm_free_page(sq_phys) calls in nvme_setup_io_queues() with pmm_free_pages(sq_phys, 4) to match the pmm_alloc_pages(4) call. Similarly, update nvme_free_queue() to accept a page count parameter or store page counts in nvme_queue_t.
- **[P2 c=0.85 fillable]** Command ID counter never wraps or resets — `kernel/drivers/storage/nvme.c:683-685, 127`
  - nvme_get_cid() returns next_cid++ without any bounds checking or wrap-around logic. The NVMe spec limits CIDs to 16-bit values (0-65535). If a long-lived controller processes many commands, next_cid will overflow, wrapping to 0 and causing command ID collisions. While unlikely during boot, in a running system with sustained I/O, duplicate CIDs can cause completion confusion and command tracking er
  - *fix:* Add wrap-around logic: `return (ctrl->next_cid++) & 0xFFFF;` to ensure CIDs stay within 16-bit range. Alternatively, initialize next_cid to a random value at controller init time to reduce collision likelihood if the counter is ever accessed after overflow.
- **[P2 c=0.75 fillable]** Missing validation of buffer physical contiguity for PRP usage — `kernel/drivers/storage/nvme.c:880-908, 913-941`
  - nvme_read() and nvme_write() assume kernel buffers are identity-mapped and physically contiguous (comment line 889-890: 'assume identity mapping for kernel buffers'). If a buffer is fragmented across multiple physical pages or not identity-mapped, the PRP list will be incorrect, leading to out-of-bounds DMA reads/writes. No validation is performed to ensure the buffer is actually contiguous.
  - *fix:* Either: (1) enforce that read/write buffers must be single-page-allocated (check using pmm API or similar), (2) implement a generic DMA buffer allocation function that guarantees physical contiguity, or (3) accept buffers via a DMA buffer pool and track their true physical addresses. Document the constraint clearly if kernel buffers are guaranteed to be identity-mapped.

## wifi-iwl (6)
- **[P0 c=1.00]** REPLY_RXON association command entirely stubbed (control-plane TODO) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:107-109`
  - The iwl_ops_connect() function at lines 107-109 has three critical TODOs: REPLY_RXON (channel, BSSID, rates, RXON flags), REPLY_RXON_TIMING (beacon interval), and REPLY_ADD_STA (AP station + TX queues). Without these commands, the association never reaches the firmware; the driver just sets local state and the radio remains unassociated. This is scaffolded for future hardware bring-up.
  - *fix:* Implement REPLY_RXON command generation from the target BSS (struct iwl_rxon_cmd is family-specific, ~160 bytes on gen1). This requires filling channel, BSSID, basic rates, and flags from the bss parameter. Submit via iwl_send_cmd() and wait for the response. Then REPLY_RXON_TIMING and REPLY_ADD_STA must follow. This is blocked by hardware validation of the struct layouts.
- **[P0 c=1.00]** Data path TX/RX queues never allocated or wired (no REPLY_ADD_STA with queues) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:110, 200-207`
  - The driver registers netif_t with tx/rx_poll functions (iwl_netif_tx, iwl_netif_rx_poll at lines 198-207) that are hard-coded stubs returning -1 or 0. The REPLY_ADD_STA command (line 110, TODO) is responsible for creating data-path TX/RX queues in the firmware. Without this command, IP traffic cannot flow. The comment at line 145 acknowledges this is safe today only because net.c hardcodes traffic
  - *fix:* Implement REPLY_ADD_STA command to allocate and configure TX/RX queues for the AP station. Then implement iwl_netif_tx() to enqueue frames into a data TX queue and iwl_netif_rx_poll() to consume REPLY_RX frames from the RX ring. This requires full data-queue SCD scheduler setup (analogous to the command-queue init in iwl-hostcmd.c iwl_scd_cmd_queue_init) and REPLY_TX framing.
- **[P1 c=1.00 fillable]** WLAN_CONNECTED state falsely reported without real association (safety/correctness issue) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:141-150`
  - The iwl_ops_set_key() function reports WLAN_CONNECTED (line 150) after a key install succeeds, even though the preceding REPLY_RXON and REPLY_ADD_STA commands are TODOs (never executed). The long comment (lines 142-149) explicitly warns: this state is a scaffolded no-op, the radio is NOT really associated, and traffic is safe only because net.c hardcodes the wired NIC. If net.c is ever made wlan0-
  - *fix:* Only report WLAN_CONNECTED after confirming REPLY_RXON/REPLY_RXON_TIMING/REPLY_ADD_STA have all succeeded and the firmware has acknowledged the association. Alternatively, keep this scaffolded but add a BUILD-TIME or runtime guard to prevent net.c from using wlan0 until the real commands are wired.
- **[P1 c=1.00]** SET_KEY command never sends actual key material to firmware (TODO) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:140`
  - The iwl_ops_set_key() function at line 140 has a TODO: REPLY_ADD_STA with the key material (PTK pairwise / GTK). Currently the function just returns 0 without submitting any command. The comment notes this is scaffolded and reports WLAN_CONNECTED, but encryption is not actually installed. Any WPA2 network would have unencrypted frames sent/received.
  - *fix:* Implement REPLY_ADD_STA command with the key material embedded (or REPLY_WEPKEY for WEP). Parse the key and index parameters and format into the appropriate command structure. This requires hardware validation of the key layout in the structure (which is family-specific).
- **[P2 c=0.95]** Disconnect command REPLY_RXON clearing never implemented (TODO) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:122`
  - The iwl_ops_disconnect() function at line 122 has a TODO to send REPLY_RXON with the assoc bit cleared to drop the link. Without this, the radio remains in the last associated state. This is less critical than the initial association but is necessary for clean disconnect.
  - *fix:* After implementing REPLY_RXON for connect, implement a variant here that sends REPLY_RXON with flags cleared (no ASSOC bit set) to tell firmware to disassociate.
- **[P2 c=0.50]** TX/RX management frame stubs not implemented (tx_mgmt, rx_poll_mgmt no-op) — `kernel/drivers/net/wireless/intel/iwlwifi/iwl-ops.c:170-175`
  - The wifi_ops functions iwl_ops_tx_mgmt() and iwl_ops_rx_poll_mgmt() (lines 170-175) are intentional no-ops because DVM firmware owns the MLME. However, if supplicant or management frames are needed in future, these stubs must be replaced with real implementations. This is not a bug, but documented as a limitation.
  - *fix:* Leave as-is for DVM (firmware owns MLME). If 802.1X/WPA supplicant frames are needed, implement via REPLY_TX and REPLY_RX_MPDU paths.

## ide (6)
- **[P0 c=1.00 fillable]** IDE app excluded from build system — `userspace/apps/Makefile:1-20`
  - The IDE application (userspace/apps/ide) contains 51 .c/.h files implementing a full development environment with compiler, assembler, parser, and semantic analyzer, but is completely omitted from the build system. The Makefile only builds terminal, taskmanager, and files apps. This architectural gap means the IDE is dead code and cannot be tested or deployed.
  - *fix:* Add 'ide' target to userspace/apps/Makefile: add 'ide' to the .PHONY declaration and the 'all' target dependencies, then add a rule 'ide:\n\t$(MAKE) -C ide/' and add 'ide clean' to the clean target.
- **[P1 c=0.92]** Single-file compilation limits multi-file projects — `userspace/apps/ide/tc.h:1-47`
  - The native toolchain (tc_build in tc_driver.c) compiles a single .c file at a time using the inline assembler/linker pipeline (C->AST->asm->machine code->ELF). There is no linker support for combining multiple object files or linking against external libraries. While the IDE's semantic model (ide_parse.c) can analyze multiple .c files in a directory (ide_parse_project_model), the build system cann
  - *fix:* Either (1) extend tc_driver.c to compile all .c files in the project directory, concatenate their .text sections, deduplicate symbols, and produce a single ELF, or (2) implement a simple multi-pass assembler that handles symbol tables and relocation. This requires significant engineering given the freestanding constraints.
- **[P1 c=0.90]** No debugger infrastructure despite runtime analysis support — `userspace/apps/ide:ide_runtime.c (exists but unused)`
  - The IDE has comprehensive runtime analysis infrastructure (ide_runtime.c, VIZ-3 RUNTIME tab, FlowStep model, flow[] array in the Model) for single-stepping and execution tracing, but there is no SYS_PTRACE or breakpoint mechanism. The visual UI for tracing exists but is disconnected from actual kernel debugging support, leaving a significant vision-vs-reality gap. Users see a RUNTIME tab that sugg
  - *fix:* Either (1) implement SYS_PTRACE syscall support in the kernel and wire it into ide_runtime.c, or (2) remove the non-functional RUNTIME tab and runtime analysis code to reduce confusion. Option 2 is safer for a from-scratch OS with limited kernel resources.
- **[P1 c=0.88]** No package/library dependency system exists — `userspace/apps/ide/ide_library.h:1-50`
  - The IDE has a snippet library (ide_library.h) with hard-coded built-in code blocks, but no package/dependency management. Projects cannot declare dependencies on external libraries (wl, bitfont, etc.) nor can the IDE help resolve them. The single-file compiler cannot link against external symbols, making it impossible to build projects that use the kernel's graphics or font libraries.
  - *fix:* Design a minimal package manifest format (e.g., project.json with 'dependencies' field), implement a library searcher that scans /usr/lib/{include,lib} for header files and prebuilt objects, and extend tc_driver.c to link against them. This is a major feature requiring OS-level package management coordination.
- **[P2 c=0.95 fillable]** Include directive path includes quote characters — `userspace/apps/ide/ide_parse.c:190-197`
  - The #include preprocessor parsing extracts the include path but includes the literal opening quote/angle-bracket character at inc->path[0]. At line 190, pstart points to the quote; at line 194, plen = pos - pstart includes the quote; at line 196, the full range from pstart is copied including the quote. This causes include paths like '"stdio.h"' to be stored as '\"stdio.h\"' (13 chars instead of 8
  - *fix:* Change line 190 to 'int pstart = pos + 1;' to skip the opening quote, or adjust the copy loop to skip pstart on line 196: change to 'for (int i = 0; i < plen; i++) inc->path[i] = src[pstart + 1 + i];' (requires adjusting plen calculation).
- **[P2 c=0.85 fillable]** ide_itoa() lacks output buffer bounds checking — `userspace/apps/ide/ide_sys.c:105-114`
  - The integer-to-ASCII conversion function ide_itoa(int v, char* out) has no capacity parameter and writes to 'out' without bounds checking on line 111 ('out[len++] = ...'). While all current callers provide 16-byte buffers and the function is designed for small integers (caret position, line numbers), this is a latent buffer overflow vulnerability if reused elsewhere or if integer ranges are expand
  - *fix:* Add an 'int cap' parameter to the function signature. Bounds-check line 111: 'if (len < cap - 1) out[len++] = ...' to prevent overflow, similar to ide_strlcpy's pattern.

## ext2 (6)
- **[P0 c=0.99 fillable]** File operations not wired to ext2 inodes during open — `kernel/fs/vfs.c:844-851`
  - When opening an ext2 file, vfs_open() always assigns ramfs_file_ops to non-device inodes, never ext2_file_ops. This causes all ext2 file reads to use ramfs_read (expecting inode->data to be in-memory), not ext2_vfs_read (which correctly reads from blocks). Ext2 files are unreadable.
  - *fix:* In vfs.c around line 844, check if inode->ops is ext2_inode_ops and assign the corresponding file_ops (either by adding a fops field to inode_ops struct, or by checking inode->sb->type == "ext2" and assigning ext2_file_ops directly).
- **[P1 c=0.95 fillable]** Block index integer overflow on large file offsets — `kernel/fs/ext2.c:300-301`
  - When reading from large file offsets, `uint32_t block_index = (file->offset + bytes_read) / fs_data->block_size;` silently truncates to 32-bit. file->offset is uint64_t; for offsets >= 4GB, block_index wraps, reading wrong blocks and leaking file data or exposing uninitialized memory. The implicit cast to uint32_t loses the high 32 bits.
  - *fix:* Cast to uint64_t before division: `uint64_t offset = file->offset + bytes_read; uint32_t block_index = (uint32_t)(offset / fs_data->block_size);` with bounds check that offset doesn't exceed inode size (already checked via available calculation).
- **[P2 c=0.85 fillable]** Missing private_data cleanup if kmalloc fails in ext2_inode_to_vfs — `kernel/fs/ext2.c:417-421`
  - If kmalloc(sizeof(ext2_inode_t)) fails at line 417, private_data remains NULL. The vfs_inode is still returned and later used in ext2_vfs_lookup (line 437) and ext2_vfs_read (line 274), which both require non-NULL private_data. This causes NULL pointer dereference or silent failures during directory traversal/file reads.
  - *fix:* Either (a) check if private_inode is NULL and vfs_inode_free() + return NULL, or (b) keep a pre-allocated copy inline in the VFS inode structure if optional, or (c) ensure the kmalloc cannot fail in the allocation pattern used.
- **[P2 c=0.70 fillable]** Write operation completely stubbed — `kernel/fs/ext2.c:345-347`
  - ext2_vfs_write() always returns -1 (error). The filesystem is read-only, which is intentional, but this is not enforced at mount-time (no read-only flag check) or during open. A user may attempt to write to an ext2 file and get no meaningful error feedback.
  - *fix:* Either enforce read-only at mount validation (reject O_WRONLY/O_RDWR flags in vfs_open for ext2 filesystems), or return a specific error code like -EROFS instead of generic -1. Document the read-only limitation in the mount function.
- **[P3 c=0.75 fillable]** Missing inode operations (create, mkdir, unlink, rmdir) not stubbed — `kernel/fs/ext2.c:30-36`
  - All write-related inode operations (create, mkdir, unlink, rmdir) are NULL pointers. If any code attempts to call them (e.g., via vfs layer), NULL function pointer dereference occurs instead of a safe error return. Symlink and device files are also not fully supported.
  - *fix:* Add explicit stub functions that return -EROFS (read-only filesystem error) for all write operations, or add checks in VFS layer before dispatching to NULL function pointers.
- **[P3 c=0.65]** Per-block kmalloc churn in indirect block lookups — `kernel/fs/ext2.c:143-256`
  - ext2_get_block_num() allocates and frees one fs_data->block_size buffer per indirect block traversal step. A triple-indirect block lookup allocates 3 separate buffers sequentially. This creates unnecessary memory pressure and fragmentation on systems with many large files. A single shared buffer or caching would be more efficient.
  - *fix:* Pass a pre-allocated block buffer to ext2_get_block_num() as a parameter, or use a small LRU cache of recently-read indirect blocks in the fs_data structure (trade-off: adds complexity vs. simplicity).

## usb (6)
- **[P0 c=0.98 fillable]** Multiple HID devices corrupt frame list schedule - only last device polled — `kernel/drivers/usb/uhci.c:572-573`
  - In hid_setup_interrupt(), every call to enumerate a new HID device replaces all 1024 frame-list entries with the *current* HID device's interrupt QH. This destroys the previously-set-up schedule for earlier devices. The initial setup at line 304 points all frames to ctrl_qh. The first device at line 573 overwrites them all to point to hid[0]->int_qh. The second device overwrites them all again to 
  - *fix:* Build a linked schedule chain instead of replacing: after creating int_qh, link it into the existing chain (e.g., point all frames to hid[0]->int_qh, then have hid[0]->int_qh->link point to hid[1]->int_qh, then to ctrl_qh). Alternatively, distribute devices across the frame list using a per-device frame offset (e.g., hid[i]'s QH at frames 0..255, hid[i+1]'s at 256..511, etc.). A safe one-liner fix
- **[P0 c=0.95]** HID interrupt callback does not resubmit transfers - data stops after first report — `kernel/drivers/usb/hid.c:415`
  - The hid_interrupt_callback() function processes HID reports but then terminates without resubmitting the interrupt transfer. This means HID keyboards/mice only generate input from the first interrupt report, then stop responding. The comment at line 415 explicitly states 'Resubmit transfer (in a real implementation, USB core handles this)' — but the USB core's usb_interrupt_transfer() is just a st
  - *fix:* Add actual transfer resubmission logic: either (1) have usb_interrupt_transfer store the callback and resubmit after the callback returns, OR (2) have hid_interrupt_callback directly call usb_interrupt_transfer() again to arm the next transfer. Since usb_core.c::usb_interrupt_transfer is a stub, the faster fix is to add resubmission in hid_interrupt_callback() itself, wrapping it in a guard to avo
- **[P1 c=0.98]** usb_interrupt_transfer() is a stub that never invokes callbacks or drives I/O — `kernel/drivers/usb/usb_core.c:172-184`
  - The usb_interrupt_transfer() function (lines 172-184 in usb_core.c) accepts a callback parameter but never stores, invokes, or integrates with any interrupt processing. It just prints a debug message and returns 0. All HID device initialization in hid.c calls this function expecting the callback to be invoked on each interrupt, but the callback never runs. This is the root cause why HID data never
  - *fix:* Either (a) have usb_interrupt_transfer() store the callback and integrate with uhci_poll() so that each polled endpoint invokes its callback with fresh data, OR (b) remove the callback abstraction entirely and have hid.c directly call uhci_poll()-like functions to check for new data. Current design has two broken layers: a stub USB core that does nothing, and a real UHCI layer that ignores the cal
- **[P1 c=0.90]** usb_core stub blocks HID descriptor retrieval - forces fallback to boot protocol — `kernel/drivers/usb/usb_core.c:162-165`
  - The usb_control_transfer() stub in usb_core.c deliberately returns -1 (failure) for GET_DESCRIPTOR requests with descriptor type REPORT. The comment says 'In real implementation, this would return the actual descriptor'. This forces hid.c::hid_probe() to always fall back to boot protocol mode (lines 224-236 in hid.c), even if a real HID report descriptor is available. Vision: a full HID parser cou
  - *fix:* Integrate usb_core.c's control transfer with the real uhci_control_transfer() implementation in uhci.c. Either (a) have usb_control_transfer() call uhci_control_transfer() directly with forwarded parameters, or (b) remove the usb_core.c stub entirely and let callers invoke uhci_init()/uhci_control_transfer() directly. The stub design creates a fake 'USB core' layer that contradicts the real hardwa
- **[P2 c=0.75 fillable]** HID device enumeration does not check for enumeration/address assignment failures — `kernel/drivers/usb/uhci.c:684-689`
  - In enumerate_port(), SET_ADDRESS (lines 684-689) can fail with rc < 0. The code logs the failure and returns -1, which is correct. However, the delay at line 690 (5000 us = 5ms) is unconditional even on failure — the code continues to use the stale hid->addr value (still 0) in the subsequent descriptor reads at lines 694-725. A device that fails SET_ADDRESS will trigger cascading failures in GET_D
  - *fix:* Add explicit error handling: (1) after SET_ADDRESS fails, return -1 immediately without proceeding to subsequent transfers. (2) Validate descriptor lengths before accessing fields (e.g., check rc >= 18 before accessing buf[7] for maxpkt). (3) Add bounds checks on the configuration descriptor blob length (line 712 caps at 64 bytes, but doesn't validate against rc). A safe one-liner: insert 'if (rc 
- **[P2 c=0.70 fillable]** Control transfer TD pool can overflow if data stage exceeds 30 packets — `kernel/drivers/usb/uhci.c:444`
  - The TD pool allocation at line 287 allocates one 4KB page, which at 32 bytes per TD allows ~128 TDs. However, the control transfer data loop at lines 435-445 silently breaks after 30 TDs (if (n >= 30) break). This truncates large control transfers (e.g., a 64-byte report descriptor with 8-byte maxpkt requires 8 data TDs + 1 SETUP + 1 STATUS = 10 TDs, which fits, but a 256-byte descriptor would nee
  - *fix:* Either (a) increase the limit from 30 to match the actual page size (e.g., 120 TDs), or (b) allocate multiple pages per control transfer and link them dynamically. A quick fix: change 'if (n >= 30)' to 'if (n >= 120)' to match a ~4KB allocation, but ensure actual_len computation at line 483 doesn't overflow. Alternatively, add a debug message or return an error code if truncation occurs so failure

## futex (6)
- **[P0 c=0.95 fillable]** Direct physical address dereference without PHYS_TO_DIRECT macro — `kernel/core/syscall/futex.c:141, 148, 155, 169`
  - futex_get_phys_addr directly casts physical addresses from CR3 and page table entries to kernel virtual pointers without using PHYS_TO_DIRECT. This causes page faults on systems where the process's identity map doesn't cover kernel page table frames (e.g., after exec splits huge pages). The code comment at line 127 even acknowledges this is not production-ready ('simplified version; production cod
  - *fix:* Use PHYS_TO_DIRECT() macro (defined in kernel/include/mem.h) to convert all physical addresses before dereferencing them. Example: `uint64_t* pml4 = (uint64_t*)PHYS_TO_DIRECT(cr3);` and `uint64_t* pdpt = (uint64_t*)PHYS_TO_DIRECT(pml4[pml4_idx] & ~0xFFFULL);` etc.
- **[P1 c=0.90]** FUTEX_WAIT_TIMEOUT syscall returns ENOTSUP instead of implementing timeout — `kernel/core/syscall/futex.c:297-303`
  - FUTEX_WAIT_TIMEOUT (op=2) is stubbed to return ENOTSUP. The timeout parameter is silently ignored (line 279), and the syscall never actually implements deadline-based blocking. Any userspace code relying on futex timeouts will fail. The comment acknowledges futextest expects ENOTSUP, but this is incomplete functionality that blocks real-world futex mutex implementations.
  - *fix:* Implement FUTEX_WAIT_TIMEOUT by passing the timeout value (3rd argument, renamed to deadline) to wait_object_block instead of 0. The function signature should map timeout to an absolute deadline: `deadline = timer_get_ticks() + timeout;` then call `wait_object_block(&bucket->wq.wobj, deadline);` instead of the current unconditional return ENOTSUP.
- **[P1 c=0.60 fillable]** futex_validate_addr does not actually validate page mapping — `kernel/core/syscall/futex.c:118-120`
  - futex_validate_addr only checks alignment and userspace bounds, but the comment explicitly says 'For now, we trust the address; page fault will catch unmapped access'. This means an unmapped user address passes validation. The actual protection relies on futex_get_phys_addr returning 0 on unmapped pages, but this is a flimsy contract. If futex_get_phys_addr fails (returns 0 for any reason), both f
  - *fix:* Either (1) add actual page-mapping validation in futex_validate_addr using paging_get_pte before futex_get_phys_addr, or (2) document clearly that futex_get_phys_addr returning 0 is the primary validation and callers should check it (already done, so this is low-priority). Consider using paging_get_pte to pre-validate the page is mapped and user-accessible.
- **[P2 c=0.75 fillable]** Lazy futex subsystem initialization race on SMP — `kernel/core/syscall/futex.c:82-93, 282-284`
  - futex_init is called lazily on first sys_futex invocation (lines 282-284) without synchronization. On SMP, multiple CPUs could simultaneously enter sys_futex before futex_initialized is set, causing redundant calls to futex_init and potential races during hash table initialization. While wq_init is idempotent, the pattern is racy and fragile.
  - *fix:* Call futex_init() early during kernel initialization (e.g., from kernel_main or a subsystem init function) instead of lazily. Remove lines 282-284. Alternatively, use a spinlock + double-check pattern: acquire lock, test futex_initialized, init if needed, release lock.
- **[P2 c=0.70 fillable]** futex_key not cleared when process is killed while parked — `kernel/core/sched/waitqueue.c:116-124`
  - wait_object_abort unlinks a killed process from the wait queue but does not clear process_t.futex_key. If a process is SIGKILL'd while in futex_wait, the futex_key persists as stale state on the PCB. While this doesn't cause a correctness bug (killed processes are TERMINATED and won't be woken by futex_wake), it's semantically incorrect and violates the invariant that non-futex-waiting processes s
  - *fix:* Add `proc->futex_key = 0;` after unlinking in wait_object_abort, or more cleanly, clear it in wo_unlink when wait_on is cleared (line 96): `it->futex_key = 0;`
- **[P3 c=0.65 fillable]** Hash collision vulnerability in futex hash table (small FUTEX_HASHSIZE) — `kernel/core/syscall/futex.c:69-71`
  - FUTEX_HASHSIZE is only 256 (1 << 8). For systems with many processes or shared memory futexes, this small hash table will have severe collisions, forcing futex_wake to scan long lists of non-matching waiters for the correct futex_key. While the key-filtering logic (FUTEX-B) correctly skips wrong waiters, heavy collisions degrade performance and could enable DoS via excessive futex creation. The ha
  - *fix:* Increase FUTEX_HASHSIZE to 2048 or 4096 (1 << 11 or 1 << 12) for typical workloads, and consider making it configurable via a boot parameter. Improve the hash function: use a proper mixing function (e.g., from Linux kernel's hash implementation) or use XOR folding of more address bits.

## block-ahci (6)
- **[P0 c=0.95 fillable]** DMA page leak on ahci_init_port failure — `kernel/drivers/storage/ahci.c:256-270, 303-315`
  - When ahci_init_port() allocates DMA pages (cmd_list, rx_fis, dma_bounce, cmd_tables) but then fails during subsequent initialization steps (device timeout at line 303, command engine start at line 309, device detection at line 312, or identify at line 315), the allocated PMM pages are never freed. This causes a memory leak of multiple 4KB pages (1 + 1 + 1 + 32 = 35 pages per failed port) when cont
  - *fix:* Add a cleanup section in ahci_init_port that calls pmm_free_page() on any successfully allocated pages before returning false. This can be implemented via a goto cleanup label or a helper function that tracks which allocations succeeded. The cleanup should free: dma_bounce (if allocated), rx_fis (if allocated), cmd_list (if allocated), and any cmd_tables[0..i-1] that were allocated before the fail
- **[P1 c=0.88 fillable]** Sector size mismatch in syscall layer — `kernel/core/syscall/handlers.c:2365, 2401`
  - The syscall handlers sys_blk_read() and sys_blk_write() hardcode sector size as 512 bytes when computing buffer length (len = count * 512). However, the AHCI driver supports variable sector sizes (see ahci.c line 488-502), which can be 512, 4096, or other values reported by the device. This causes buffer overruns when count=256 and sector_size=4096: the syscall allocates only 128KB but needs 1MB, 
  - *fix:* Query the actual sector size from the AHCI port before computing len. Replace hardcoded 512 with ahci_get_sector_size() or similar, or pass the sector size to ahci_read/write. Alternatively, reject requests with sector_size != 512 at the syscall boundary to match the documented contract.
- **[P1 c=0.75 fillable]** No bounds check in ahci_port_free_slot — `kernel/drivers/storage/ahci.c:387-389`
  - The ahci_port_free_slot() function does not validate the slot parameter before using it in a bit-shift operation (port->slot_bitmap &= ~(1u << slot)). If called with slot >= 32 or with an unintended negative value (though current callers appear safe), this causes undefined behavior and potential corruption of adjacent memory or register fields in the port structure.
  - *fix:* Add a bounds check at the start of ahci_port_free_slot: if (slot < 0 || slot >= AHCI_MAX_CMD_SLOTS) return; This is defensive even though current callers are safe, because it prevents future misuse.
- **[P2 c=0.85 fillable]** Uninitialized ahci_controller kmalloc failure leaves no error path — `kernel/drivers/storage/ahci.c:676-680`
  - In ahci_init(), if ahci_probe_controller() succeeds but ahci_init_controller() fails (line 681), the allocated controller structure (from kmalloc on line 153) is never freed. This is a single-instance leak (unlike the port DMA leak), but combined with other failures can cause kernel memory exhaustion. Also, if ahci_init_controller() fails, the loop at line 218 may have partially initialized ports 
  - *fix:* Add cleanup before each early return after controller allocation: if (!ahci_init_controller(...)) { kfree(controller); ... return -3; }. Alternatively, move the kfree call to the end of ahci_init in a cleanup section.
- **[P2 c=0.65 fillable]** ahci_port_alloc_slot no validation of controller/port pointer — `kernel/drivers/storage/ahci.c:376-385`
  - The ahci_port_alloc_slot() function does not check if port is NULL before dereferencing port->regs. If a caller somehow passes NULL (e.g., during error recovery or in multi-port scenarios), this causes a NULL pointer dereference. While current call sites do not pass NULL, this is a robustness gap.
  - *fix:* Add a NULL check at the start: if (!port) return -1; This ensures defensive robustness without changing the API.
- **[P3 c=0.72 fillable]** Block device layer does not reject count=0 reads/writes before calling driver — `kernel/drivers/storage/block.c:69, 97`
  - When a caller invokes block_read() or block_write() with count=0, the bounds check on line 69/97 allows it to pass (0 is not > sector_count), and the operation is forwarded to the driver. While ahci_read_sectors() correctly rejects count=0 on line 559/581, this is a semantic violation: zero-length I/O should be rejected at the block layer before reaching the driver. This also increments dev->error
  - *fix:* Add an explicit count==0 check before the bounds check in block_read() and block_write(): if (count == 0) { kprintf(...); return false; } or similar, to short-circuit before driver callback.

## tls-crypto (6)
- **[P1 c=1.00]** TLS 1.3 not implemented — `userspace/lib/tls/tls.h:2`
  - This is a TLS 1.2-only client implementation (RFC 5246). TLS 1.3 (RFC 8446) is not implemented, meaning connections to modern TLS 1.3-only servers will fail. While this is documented as intentional, it represents a significant gap for production deployments on modern internet infrastructure where TLS 1.3 is widely adopted.
  - *fix:* Implement TLS 1.3 handshake state machine, key schedule (HKDF-based), PSK modes, 0-RTT support, post-handshake authentication, and new record format. This is a substantial engineering effort (thousands of lines) requiring new protocol state logic, different key derivation, and HelloRetryRequest handling.
- **[P1 c=0.95 fillable]** RSA ServerKeyExchange signature verification rejects SHA384/512 despite RSA support — `userspace/lib/tls/tls.c:1335`
  - The verify_ske_signature() function explicitly rejects RSA ServerKeyExchange signatures with SHA384 or SHA512, returning TLS_ERR_SIG. While the underlying RSA PKCS#1 v1.5 verification in crypto/rsa.c supports SHA384/512 DigestInfo prefixes, the TLS handshake layer artificially limits ServerKeyExchange signature verification to SHA-1 and SHA-256 only. This means legitimate servers using SHA384/512 
  - *fix:* Replace lines 1331-1335 with logic that accepts SHA384/512 hashes and adds proper RSA_HASH_SHA384/512 encoding. For SHA384/512, construct the correct DigestInfo (already defined in rsa.c) and pass RSA_HASH_SHA384 or RSA_HASH_SHA512 to rsa_pkcs1_verify().
- **[P2 c=0.90 fillable]** ECDSA ServerKeyExchange signature verification only supports SHA256 (not SHA384/SHA512) — `userspace/lib/tls/tls.c:1341-1361`
  - When verifying ECDSA ServerKeyExchange signatures, the code supports SHA256 hashes but implicitly passes any hash length (32, 48, or 64 bytes) directly to p256_ecdsa_verify() which uses the leftmost 256 bits of any hash. While p256_ecdsa_verify() technically accepts SHA384/512, no ECDHE_ECDSA_*_SHA384/512 cipher suites are advertised in ClientHello (only SHA256), so servers sending SHA384/512 ECDS
  - *fix:* Add ECDHE_ECDSA_WITH_AES_256_GCM_SHA384 and related ECDSA-SHA384 cipher suites to the advertised list in build_client_hello() and add decode_suite() cases for them. Ensure proper hash algorithm selection in the PRF and Finished message computation.
- **[P2 c=0.90 fillable]** x509_verify_chain hard-codes ECDSA certificate validation to SHA256 only — `userspace/lib/tls/x509_verify.c:101, 356-357, 517`
  - The x509_verify.c module defines only SIGALG_ECDSA_SHA256 (no SHA384/512 variants) and hard-codes the hash length to 32 bytes at line 517: 'if (p256_ecdsa_verify(point, hash, 32, r, s) != 0)'. The cert parsing recognizes OID_ECDSA_SHA256 but rejects ECDSA-SHA384 and ECDSA-SHA512, making it impossible to validate certificate chains signed with ECDSA-SHA384/512. While p256_ecdsa_verify() can technic
  - *fix:* Add SIGALG_ECDSA_SHA384 and SIGALG_ECDSA_SHA512 constants, recognize their OIDs, and pass the actual hash length (48 or 64) to p256_ecdsa_verify() instead of hard-coding 32. Ensure the signature_verify() function branches on the actual algorithm's hash size.
- **[P2 c=0.85 fillable]** Certificate chain parsing stops validating structure once TLS_MAX_CHAIN limit reached — `userspace/lib/tls/tls.c:1154-1162`
  - The parse_certificate() function stops parsing the server's certificate list once chain_count reaches TLS_MAX_CHAIN (8 certs), breaking out of the loop at line 1162. The comment at line 1159 claims to 'keep parsing to validate the structure', but the break statement prevents validation of any remaining certificates beyond the 8th. A malformed/truncated cert in position 9+ would not be caught, only
  - *fix:* Remove the break at line 1162 and change the loop to continue validating the structure of all remaining certificates even after chain_count >= TLS_MAX_CHAIN. Move the break outside the loop condition or use a flag to distinguish between collecting (capped) vs. validating (exhaustive).
- **[P3 c=0.95]** No support for client certificate authentication (ClientCertificate, CertificateVerify) — `userspace/lib/tls/tls.c:1517-1521`
  - The TLS 1.2 client handles HS_CERTIFICATE_REQUEST messages by skipping them (line 1517-1521) with a comment that client auth is not supported and the server 'may make it optional'. While documented, this is a real limitation: servers that require client certificates will fail the handshake. The code never generates a Certificate or CertificateVerify message, meaning mutual authentication / mTLS is
  - *fix:* Implement client certificate support: (1) detect whether a CA/certificate list is available, (2) select a matching client cert from a store, (3) send Certificate handshake message, (4) compute and send CertificateVerify message signing the handshake transcript with the client's private key. This requires integration with a local cert store and private-key access.

## libc-stdio (6)
- **[P1 c=0.99]** scanf/sscanf completely stubbed - always return EOF — `userspace/libc/stdio.c:807-828`
  - fscanf(), scanf(), and sscanf() are stub implementations that unconditionally return EOF without parsing format strings or reading input. This breaks all formatted input functionality. Line 812 explicitly documents 'For now, just a stub that returns EOF'.
  - *fix:* Implement scanf/sscanf with format string parsing (at minimum %d, %s, %x support), variable argument extraction via va_list, and character-by-character input parsing from FILE/string buffer.
- **[P1 c=0.98 fillable]** exit() does not call atexit handlers — `userspace/libc/syscall.c:26-30`
  - exit() directly calls SYS_EXIT without invoking registered atexit handlers via __call_atexit_handlers(). The handlers are registered (stdlib.c line 746), but exit() never calls them. This violates C standard behavior where exit() must flush streams and run atexit callbacks before terminating.
  - *fix:* In exit() (syscall.c:26), call __call_atexit_handlers() before the syscall6(SYS_EXIT,...) invocation. Also add fflush(NULL) to flush all open streams.
- **[P2 c=0.99]** lseek() always fails - stub returns -1 — `userspace/libc/syscall.c:81-88`
  - lseek() is a stub that unconditionally returns -1. This breaks fseek(), ftell(), and all file positioning in buffered I/O. Line 82-83 comment confirms: 'lseek syscall doesn't exist yet in kernel'. File operations that rely on seeking (e.g., fseek in stdio.c lines 642-666) will fail silently.
  - *fix:* Implement SYS_LSEEK syscall in the kernel (or verify if it exists under a different number), then wire it into lseek() with proper argument marshalling.
- **[P2 c=0.95 fillable]** abort() does not send SIGABRT signal - just exits — `userspace/libc/stdlib.c:727-729`
  - abort() calls exit(134) instead of raising SIGABRT (signal 6). The exit code 134 is a Unix convention meaning 'terminated by SIGABRT', but abort() should actually deliver SIGABRT to the process so signal handlers can run and core dumps can be generated. Current implementation bypasses signal delivery entirely.
  - *fix:* Change abort() to call raise(SIGABRT) first (which invokes the handler), then exit(1) as a fallback if the handler returns (which shouldn't happen per POSIX). Alternatively, use kill(getpid(), SIGABRT).
- **[P2 c=0.92 fillable]** strtol/strtoll lack overflow detection - silent wraparound — `userspace/libc/stdlib.c:207-212`
  - strtol() at line 209 performs result = result * base + char_to_digit(*p) without checking for overflow. Parsing a very large number like '9999999999999999999999' silently wraps to a negative value with no indication. C99 requires setting errno=ERANGE on overflow and clamping to LONG_MIN/LONG_MAX.
  - *fix:* Before each digit addition, check if result would exceed LONG_MAX (after accounting for sign). Set errno to ERANGE and clamp to LONG_MAX/LONG_MIN if overflow detected. Same fix needed in parse_unsigned() at line 274.
- **[P2 c=0.88]** ftell() reports incorrect position with buffering - ignores unconsumed data — `userspace/libc/stdio.c:668-689`
  - ftell() calls lseek(fd, 0, SEEK_CUR) but since lseek() always returns -1 (stub), ftell() always returns -1 instead of the actual position. Additionally, if lseek were fixed, the buffering adjustment logic (lines 680-686) only corrects for read-mode unconsumed data and write-mode unflushed data, but does not account for partially-flushed buffers or mixed read/write sequences.
  - *fix:* Fix lseek() stub first (gap #5). Then verify ftell() buffering math handles all read/write/mixed scenarios correctly.

## cc-back (6)
- **[P1 c=0.99]** Indirect function calls explicitly unsupported, no error recovery — `userspace/apps/ide/cc_expr.c:864-870`
  - Lines 864-870 explicitly reject indirect calls (via function pointer or expression). The code checks `if (callee && callee[0])` which requires a simple identifier name. Any attempt to call through a pointer (e.g., `(*fp)()` or `arr[i]()`) results in 'only simple-named calls supported' error. This is a hard restriction that blocks callback patterns and function tables.
  - *fix:* To support indirect calls: generate code to evaluate the callee expression into rax, then emit 'call rax' (using the assembler's existing support for register-indirect calls, or add it). Ensure RSP alignment is maintained around the indirect call.
- **[P1 c=0.95]** Switch/case/default statements parsed but not code-generated — `userspace/apps/ide/cc_codegen.c:484-486`
  - AST_SWITCH, AST_CASE, and AST_DEFAULT node kinds are successfully parsed (ide_pstmt.c lines 305-350) but fall through to the default 'unsupported statement' error in cc_codegen's gen_stmt() switch at line 484. Code will compile but fail at codegen time with 'unsupported statement' diagnostic, losing the source-to-ELF pipeline for any program using switch statements.
  - *fix:* Implement gen_switch() handler in cc_codegen.c that: (1) evaluates the switch selector into rax, (2) generates a series of cmp/jne for each case label (or a jump table if many cases), (3) jumps to the matched case or default, (4) sets break_label so 'break' jumps to the end. Handle fall-through between cases.
- **[P2 c=0.95]** Do-while loops parsed but not code-generated — `userspace/apps/ide/cc_codegen.c:484-486`
  - AST_DO nodes (parsed at ide_pstmt.c lines 247-258) are not handled in gen_stmt(), causing 'unsupported statement' error at codegen. The do-while syntax is fully parsed but rejected at the backend, blocking programs that use it.
  - *fix:* Implement gen_do() in cc_codegen.c similar to gen_while() but with the loop test at the bottom: emit loop label, emit body, emit condition test, conditional jump back to top. Set break/continue labels correctly.
- **[P2 c=0.90]** Goto/label statements parsed but not code-generated — `userspace/apps/ide/cc_codegen.c:484-486`
  - AST_GOTO and AST_LABEL nodes (parsed at ide_pstmt.c lines 384-420) reach the default case in gen_stmt(), causing rejection. Label/goto support requires a two-pass approach or forward-reference tracking which is not implemented.
  - *fix:* To support labels and goto: (1) collect all labels in a first pass before statement generation, (2) during codegen, emit label definitions at their statements and generate absolute jumps for goto. Alternatively, forward-declare all labels at function prologue.
- **[P3 c=0.75 fillable]** ELF header fields validated at write time only, no pre-validation of code size — `userspace/apps/ide/elf_write.c:116-122`
  - The elf_write() function validates code_len and capacity (lines 116-122), but it does so only at ELF emission time. There is no pre-check that the assembled code fits in TC_CODE_CAP (64KB) before as_assemble() is called. If assembly produces >64KB of code, the overflow will be caught by elf_write() with a cryptic -2 return, not at assembly time. The user sees 'ELF write failed' rather than 'code t
  - *fix:* Add a check in tc_driver.c after as_assemble() returns: verify code_len < TC_CODE_CAP and emit a diagnostic 'generated code exceeds capacity (code_len > max)' if exceeded. Alternatively, pass TC_CODE_CAP to as_assemble() and have it detect and report the overflow at assembly time.
- **[P3 c=0.70 fillable]** String literal deduplication lacks overflow hardening for edge case — `userspace/apps/ide/cc_codegen.c:168-187`
  - The cc_intern_str() function (lines 168-187) deduplicates strings by checking all existing entries. The string text is copied with cc_strcpy() which is safe. However, if CC_MAXSTR (64) is reached, the function logs an error and returns -1, but callers (line 516 in cc_expr.c) do not validate the return value before using it as an index. An ID of -1 passed to later code could cause a buffer underflo
  - *fix:* In cc_expr.c line 516-520: check if id < 0 (intern failed) and emit an error instead of proceeding to use a negative array index. Alternatively, pre-check string count in cc_collect_globals and warn early.

## browser (6)
- **[P1 c=0.99]** <script src> tags are completely ignored — `userspace/apps/browser2/browser2.c:1348`
  - browser2 only runs inline <script>...</script> code via html_get_inline_scripts(), but never fetches or executes <script src="URL"> external scripts. The parser extracts them correctly (html_get_script_srcs exists in html_parse.c:1014), but they are silently discarded. This breaks any website that relies on external JS files.
  - *fix:* At line ~1348, after extracting inline scripts, also extract and fetch external scripts via http_get/https_get, then execute them with js_eval_keep_env() in the correct document order (before/after inline scripts as they appear).
- **[P2 c=0.98]** @media queries are completely skipped — `userspace/lib/css/css.c:721-723`
  - The CSS parser detects @-rules (including @media) and explicitly skips them with skip_at_rule(). No media-query evaluation happens; all @media blocks are discarded. This means responsive CSS rules never apply, breaking mobile/tablet layouts.
  - *fix:* Implement a minimal media query evaluator (e.g. @media screen, @media print, @media (max-width: Xpx)) that checks against the current viewport width (VP_W = 800px) and applies rules conditionally. Start with the most common forms.
- **[P2 c=0.98]** No flexbox (display:flex) support — `userspace/lib/css/css.c:946-950`
  - apply_decl() handles only display: block/inline/inline-block/none. There is no case for display:flex or display:grid. The layout.h header explicitly documents this as unsupported, but code that specifies display:flex will silently fall back to inline (or be misinterpreted), breaking flex-based layouts.
  - *fix:* Add flex/grid display values to css_computed, then implement minimal flex layout in layout.c (flex-direction, flex-wrap, justify-content basics). Alternatively, emit a stub that treats flex containers as block (at least the layout won't be corrupted).
- **[P2 c=0.95]** Response.json() silently degrades to raw text — `userspace/lib/js/js_fetch.c:226-251`
  - response.json() cannot call js_eval() to parse JSON because js_eval() resets the VM arena and native-class registry, losing the DOM bindings. Rather than throw an error, it silently returns response.text() instead. This breaks code that expects .json() to parse the body: the caller gets a string, not an object. The limitation is documented but not obvious to users.
  - *fix:* Export the internal JSON parser from js_builtin.c as a public function (jp_parse or similar) that can be called directly without js_eval(). Alternatively, pre-parse common JSON responses in fetch internals by stashing them in a keyed cache during do_fetch().
- **[P2 c=0.92]** No CSS positioning support (position property) — `userspace/lib/css/css.c:904-1025`
  - The apply_decl() function (which processes CSS properties) has no case for the 'position' property. Absolute/relative/fixed positioning is never parsed or applied, even though layout.h documents this as a known limitation. Any position:absolute layout will render as position:static (default flow).
  - *fix:* Add a position property case to apply_decl() that stores position in css_computed struct, then wire layout.c to respect it (offset from normal flow for relative, out-of-flow for absolute/fixed). This requires expanding css_computed and the layout engine.
- **[P3 c=0.88]** No fetch() request retry or exponential backoff — `userspace/lib/js/js_fetch.c:382-384`
  - When http_get/https_get returns n_bytes < 0 (network failure, timeout), fetch() immediately returns an error Response with status=0 and ok=false. There is no retry loop, exponential backoff, or retry budget. This matches HTTP fetch() spec (which doesn't mandate retries), but means transient network blips immediately fail to the user.
  - *fix:* Add a configurable retry loop (e.g. 2-3 retries with exponential backoff) before do_fetch() returns an error response. Bound by the HTTP layer's timeout so retries don't exceed the user's deadline. This is optional per-spec but improves UX.

## mem-vmm (6)
- **[P1 c=0.98 fillable]** vmm_alloc_at directly dereferences physical page pointer without using PHYS_TO_DIRECT — `kernel/core/mem/vmm.c:522-525`
  - vmm_alloc_at zeros newly allocated physical pages using direct identity-map dereference (line 522: `uint8_t* page_ptr = (uint8_t*)phys`), which violates the kernel's requirement that all kernel code accessing raw physical frames must go through PHYS_TO_DIRECT(). Under a process CR3 where the identity map is mutated/split (typical during exec), the physical page may not be accessible via identity, 
  - *fix:* Change line 522 to `uint8_t* page_ptr = (uint8_t*)PHYS_TO_DIRECT(phys);` to access the page through the dedicated direct-map alias which is guaranteed accessible on any CR3.
- **[P1 c=0.95 fillable]** CR3 masking uses incomplete mask, leaving bit 63 set when CR3_NO_FLUSH is active — `kernel/core/mem/vmm.c:136, 182`
  - Two calls to read_cr3() use `& ~0xFFFULL` to extract the PML4 physical address, but this only clears bits 0-11 (PCID). When CR4.PCIDE is enabled and CR3 has bit 63 set (CR3_NO_FLUSH), the resulting pointer includes bit 63, making it non-canonical. Later dereferences (e.g., line 141, 206-213) would #GP on the non-canonical address. The correct mask is `0x000FFFFFFFFFF000ULL` as used throughout pagi
  - *fix:* Change line 136 from `(uint64_t*)(read_cr3() & ~0xFFFULL)` to `(uint64_t*)(read_cr3() & 0x000FFFFFFFFFF000ULL)`. Change line 182 similarly.
- **[P2 c=0.75 fillable]** validate_user_buffer does not check page-presence, only range bounds — `kernel/core/mem/vmm.c:57-82`
  - validate_user_buffer has a TODO comment (line 78) stating it should verify pages are actually mapped and accessible, but currently only checks address range. This is explicitly weaker than copy_from_user (which calls user_range_is_accessible at lines 354-356) and could lead to a false-positive validation if a caller relies on this function before making an unsafe memory access. However, the primar
  - *fix:* Implement the page-presence check: call user_range_is_accessible(addr, size, false) and return its result. This makes validate_user_buffer match copy_from_user's safety guarantee. Alternatively, document that callers must follow up with copy_from_user before dereferencing the buffer.
- **[P2 c=0.50 fillable]** vmm_protect saves and restores paging target without masking CR3 properly — `kernel/core/mem/vmm.c:669-692`
  - vmm_protect saves the current paging target via `read_cr3() & 0x000FFFFFFFFFF000ULL` (correct masking), but then later attempts to restore it. If the saved_target is 0 (which is falsy), the restore logic at lines 690-692 has a semantic issue: if `saved_target != kernel_cr3`, it re-applies paging_set_target(saved_target), but a value of 0 would indicate a failed read or initialization error. While 
  - *fix:* Store both the paging target and the kernel CR3 at function entry, or simplify by always calling paging_reset_target() at exit (lines 688) without the conditional re-set. The current logic suggests defensive re-entrance but is only executed once.
- **[P3 c=0.65]** vmm_alloc_at overlap check only validates first and last page, missing interior unmapped pages — `kernel/core/mem/vmm.c:492-505`
  - vmm_alloc_at checks for existing mappings by only testing the first and last pages in the range (lines 495-496), with a comment acknowledging 'A full implementation would check every page'. This could allow a allocation to partially overlap an existing mapping in the middle, silently corrupting the caller's data. The code comment is transparent about this limitation, and most callers likely consul
  - *fix:* Either (1) add a full-range VMA check before this function, (2) walk all pages in the range with a loop, or (3) consult the process's vma_rbtree to confirm the range is free. Option 3 is safest but requires access to the process struct.
- **[P3 c=0.45]** Lazy demand paging infrastructure present but no explicit TLB coherence guarantee for demand-faulted pages — `kernel/core/mem/vma_region.c:133-135`
  - handle_page_fault allocates and maps a page on demand, calling vmm_map_page which issues invlpg. However, the function documents that it runs 'under the FAULTING process's CR3, whose low identity map may not cover a high frame'. While PHYS_TO_DIRECT is used to safely populate the frame (line 99), the subsequent vmm_map_page call (line 134) targets the active_pml4, which was set to the faulting CR3
  - *fix:* Document that handle_page_fault is only safe when called with the faulting process as the currently-running CR3 (guaranteed during exception handling). Alternatively, check that the target CR3 matches read_cr3() before issuing invlpg, or skip invlpg if they differ (the TLB has no entry to flush).

## net-l3 (6)
- **[P1 c=0.98 fillable]** Missing ICMP Port Unreachable implementation — `kernel/net/udp.c:54-77`
  - UDP layer declares extern net_icmp_port_unreachable() but this function is never defined anywhere. The udp_input() function has a placeholder comment (line 74-76) noting that ICMP port unreachable cannot be sent because only the UDP segment is available, not the full IP packet needed for the ICMP quote. This violates UDP/IP protocol requirements and prevents proper error reporting when no socket l
  - *fix:* Define net_icmp_port_unreachable() in net.c that accepts the full IP packet context (or refactor ipv4_demux in socket.c to pass the original ipv4_hdr_t* to udp_input()), and wire it correctly in udp.c line 77. Alternatively, pass orig_ip + orig_len through the call chain so the ICMP quote can be constructed properly.
- **[P2 c=0.95 fillable]** Fragment reassembly timeout does not send ICMP Time Exceeded — `kernel/net/net.c:342-352`
  - frag_timeout_check() explicitly discards timed-out reassembly buffers with a comment on line 348: 'We don't have the original packet saved, so skip ICMP for now.' RFC 792 requires ICMP Time Exceeded (Type 11 Code 1) to be sent when reassembly times out. The frag_reasm_t structure does not store a copy of the first fragment's IP header needed to construct the ICMP quote, so timeouts silently drop w
  - *fix:* Add a field to frag_reasm_t (e.g., orig_ip_hdr[20] to store the IPv4 header) captured on first fragment arrival. In frag_timeout_check(), call icmp_send_error(ICMP_TIME_EXCEEDED, ICMP_FRAG_TIMEOUT, &stored_header, sizeof_stored_header) before invalidating the entry.
- **[P2 c=0.90 fillable]** ARP cache eviction policy is ineffective (evicts slot 0) — `kernel/net/net.c:141-142`
  - The arp_insert() function uses a crude FIFO replacement: when the 16-entry ARP cache is full, it unconditionally overwrites slot 0 rather than using a proper eviction strategy (LRU, LFU, or round-robin). This causes the first cached entry to be repeatedly evicted on each new address, leading to thrashing if more than 16 active neighbors exist. Additionally, marking an entry valid without checking 
  - *fix:* Implement round-robin or LRU eviction: add a timestamp to arp_entry_t or maintain a next_evict index. On cache full, evict the oldest entry or the least-recently-used one, not always slot 0. Alternatively, implement a simple per-entry timestamp: add uint64_t last_used_ms to arp_entry_t and track in arp_insert().
- **[P3 c=0.80 fillable]** ICMP echo reply captured globally without flow validation — `kernel/net/net.c:579-583`
  - The ipv4_input() function stores ANY ICMP echo reply into net.got_echo_reply, net.last_echo_id, net.last_echo_seq, net.last_echo_from without validating that it matches an outstanding echo request. An attacker (or misconfigured network) sending unsolicited ICMP echo replies will poison these globals, causing net_ping() to return immediately (false success) even if the actual destination was unreac
  - *fix:* Maintain a small queue or bitmap of outstanding echo request (id, seq) pairs sent via net_icmp_echo(). Only update net.got_echo_reply if the reply matches an outstanding request. Optionally: add a timeout to discard stale entries, or use a simple ring buffer of the last 4 requests to avoid memory cost.
- **[P3 c=0.75 fillable]** Fragment reassembly .bss cost not justified against actual workload — `kernel/net/net.c:52-95`
  - Fragment reassembly allocates 16 × (65535 data + 65535 holes bitmap) = ~2.1 MB in .bss.deferred. Comments justify this by deferring above userspace to protect critical kernel structures. However, the kernel supports at most 32 sockets (socket.h line 44) and typical workloads (QEMU user-net, local LAN) rarely encounter fragmentation. The 16-slot limit means only 16 concurrent reassemblies are possi
  - *fix:* Reduce FRAG_REASM_MAX from 16 to 8 (saves ~1 MB .bss), or implement a hybrid: keep a small in-kernel pool (4-8 slots) for critical paths and fall back to kmalloc-ed buffers for additional reassemblies. Measure actual fragmentation frequency in test workloads before deciding.
- **[P3 c=0.70 fillable]** net_send hard-codes ternary check on g_nic without explicit NIC_NONE guard — `kernel/net/net.c:613-616`
  - net_send() checks (g_nic == NIC_RTL8139) ? rtl8139_tx : e1000_tx. If g_nic is NIC_NONE (0) at initialization or after a failed init, this silently dispatches to e1000_tx, which may dereference uninitialized device state. The explicit check net.up prevents the call from proceeding, but the ternary is misleading and could silently fail if net.up is removed or bypassed elsewhere.
  - *fix:* Change the ternary to explicitly handle NIC_NONE: if (g_nic == NIC_NONE) return -1; else (g_nic == NIC_RTL8139) ? ... Alternatively, initialize g_nic to a valid default (e.g., NIC_E1000) at compile time and assert it changed during net_init().

## net-udp-dns (6)
- **[P1 c=0.98 fillable]** DNS resolver accepts responses from any source (no source validation) — `userspace/lib/net/dns.c:729, 788-810`
  - The DNS resolver uses RECVFROM (line 788-789) which populates the 'from' address struct (line 729) with the source IP of the response. However, the code never validates that this source IP matches g_dns_server (the configured resolver). This allows an attacker on the network to send spoofed DNS responses that will be accepted and cached, enabling DNS spoofing attacks. The 'from' variable is captur
  - *fix:* After line 790 (rlen = (int)n; break;), add a check: if (from.ip != g_dns_server) { continue; } to reject responses from unexpected sources. This requires minimal changes and is safe to add locally.
- **[P1 c=0.95]** Port Unreachable (ICMP) undefined and never sent — `kernel/net/udp.c:54, 73-77`
  - The function net_icmp_port_unreachable() is declared as extern at line 54 but is never defined anywhere in the codebase. When a UDP datagram arrives for a port with no listening socket, the code returns silently (line 77) instead of sending an ICMP Destination Unreachable (Port Unreachable) error. The comment explicitly states 'would need refactor' to implement this properly because the IPv4 layer
  - *fix:* Refactor the call chain to pass the full IP packet to udp_input(), or save the full packet context in the socket layer and use it here. Then implement net_icmp_port_unreachable() to call net_icmp_dest_unreach(ICMP_PORT_UNREACH, ...) as defined in net.h:241.
- **[P2 c=0.95]** UDP datagram queue overflow silently drops packets — `kernel/net/udp.c:82-83`
  - When the UDP datagram queue for a socket is full (16 slots), new incoming datagrams are silently discarded (line 83: 'if (s->dq_count >= UDP_QUEUE_DEPTH) return;'). There is no indication to the sender that the datagram was dropped, and no ICMP error is sent. This violates the principle of reliable error reporting and can cause silent data loss in applications expecting UDP reliability semantics o
  - *fix:* Either: (1) send ICMP Destination Unreachable when the queue is full, or (2) increase UDP_QUEUE_DEPTH from 16 (defined in kernel/include/socket.h:47), or (3) implement backpressure signaling to the application layer. Sending ICMP requires access to the full IP packet (same refactor as port_unreachable).
- **[P2 c=0.85 fillable]** DNS advertises 1232-byte EDNS0 payload but response buffer is only 512 bytes — `userspace/lib/net/dns.c:727-728, 769`
  - The DNS query includes an EDNS0 OPT pseudo-RR (line 765-775) advertising a 1232-byte UDP payload size (0x04D0 = 1232), following RFC 6891 recommendation. However, the response buffer is hardcoded to 512 bytes (line 728). If a resolver responds with a packet larger than 512 bytes, the data will be truncated by the kernel's UDP recvfrom() limitation. This breaks compliance with the EDNS0 advertiseme
  - *fix:* Either: (1) increase resp[] buffer to 1232+ bytes and adjust the cap in syscall limit, or (2) reduce EDNS0 payload advertisement to match the actual buffer size (e.g., advertise 512 instead of 1232). Option 1 is preferable for DNSSEC compatibility.
- **[P3 c=0.70 fillable]** DNS cache LRU eviction vulnerable to seq counter wraparound — `userspace/lib/net/dns.c:197, 275, 310-313, 325`
  - The DNS cache uses a monotonically increasing sequence counter (g_cache_seq) to track insertion order for LRU eviction. When this unsigned int counter wraps around from UINT_MAX to 0, the LRU logic (lines 310-313) compares using '<' operator on the wrapped sequence numbers. After wraparound, a very old entry with seq=0 will appear younger than a new entry with seq=UINT_MAX, causing incorrect evict
  - *fix:* Track insertion order using a 64-bit counter or reset g_cache_seq to 1 when it exceeds a large threshold (e.g., every 2^31 insertions). Alternatively, use timestamps (SYS_GET_TICKS_MS) directly for eviction which naturally handles wraparound, though with coarser granularity.
- **[P3 c=0.60 fillable]** UDP checksum calculation includes incomplete pseudo-header validation — `kernel/net/socket.c:79-101`
  - The transport checksum calculation at line 79-101 correctly includes the IPv4 pseudo-header (src IP, dst IP, proto, length) but does NOT validate the received checksum on inbound packets. The udp_input() function accepts and queues datagrams without any checksum verification. While RFC 768 permits UDP checksum to be 0 (disabled), accepting all datagrams regardless of checksum integrity reduces rob
  - *fix:* In udp_input() around line 80, add optional checksum validation: compute the expected checksum from the received UDP segment + pseudo-header, and reject the datagram if the received checksum (when non-zero) doesn't match. Can be conditional on a build flag for minimal implementations.

## cc-front (6)
- **[P1 c=0.98]** No support for #include directives; preprocessor tokens are stripped — `userspace/apps/ide/ide_pcore.c:239`
  - The parser unconditionally discards all TK_PREPROC tokens (including #include, #define, etc.) in parser_init(). There is no include-file resolution, macro expansion, or even basic directive parsing. This blocks any code that uses standard headers (e.g., `#include <stdio.h>`) or conditional compilation.
  - *fix:* Design and implement a minimal preprocessor layer: handle #include (at least search in local dir + fixed stdlib path), track #define macros for simple expansion, and preserve/validate #ifdef/#endif nesting.
- **[P1 c=0.95 fillable]** Cast expressions silently discarded in const_eval for global initializers — `userspace/apps/ide/cc_expr.c:207-268`
  - cc_const_eval() does not handle AST_CAST nodes. When a cast appears in a file-scope initializer (e.g., `int x = (int)1.0;`), it falls through to the default case and returns 0 (not constant), causing the initializer to be silently zero-filled instead of being evaluated. This is a correctness bug: C allows casts in constant expressions.
  - *fix:* Add case AST_CAST to cc_const_eval() to recursively fold the operand and return the result, potentially after width conversion.
- **[P2 c=0.92 fillable]** Multi-declarators in one statement do not dedup initializers in sibling chain — `userspace/apps/ide/ide_pdecl.c:622-656`
  - The parser correctly chains comma-separated declarators as sibling nodes (e.g., `int a, b=1, *c;`). However, the sibling chain is built by setting tail->next = v, then later the TU/block loop adds each sibling via ast_add_child(). The ast_add_child() function resets ->next to 0 (line 84 of ide_ast.c: `child->next = 0;`), breaking the chain. As a result, all declarators except the first are dropped
  - *fix:* Save and restore the ->next chain in parse_translation_unit() and parse_compound() before calling ast_add_child(), or introduce a separate mechanism (e.g., a parameter flag) to defer sibling linkage until after the tree insertion.
- **[P2 c=0.88 fillable]** Cast codegen is identity-pass; does not convert between incompatible types — `userspace/apps/ide/cc_expr.c:878-882`
  - When AST_CAST is evaluated in ce_expr(), it simply evaluates the operand and discards the cast (comment: 'v1: treat a cast as its operand'). This means `(char)(0x12345678)` truncates via normal register semantics but `(int*)(0x1234)` does not initialize the address correctly, and pointer<->int casts are bit-identical rather than zero-extended or sign-extended as needed.
  - *fix:* After evaluating the operand, emit conversion code: for pointer<->int, load/store via the target type; for int narrowing, mask/truncate; for widening, sign-extend (sext) or zero-extend (movzx) as appropriate.
- **[P2 c=0.85 fillable]** AST pool exhaustion not detected in codegen; arena_full flag not checked — `userspace/apps/ide/cc_codegen.c:500-667`
  - The AST arena has a fixed pool of 16,384 nodes (AST_POOL_CAP). When ast_new() exhausts the pool, it returns a permanent AST_NONE sentinel. The codegen functions (cc_collect_globals, cc_gen_function, etc.) do not check ast_arena_full() before or after code generation, so a large program silently fails to produce a complete AST without diagnostics.
  - *fix:* Check ast_arena_full() at the start of cc_gen_program() and emit a diagnostic if true. Alternatively, track node exhaustion during parsing and report it immediately.
- **[P3 c=0.80 fillable]** Ternary and comma operators not checked for side-effect in const_eval — `userspace/apps/ide/cc_expr.c:207-268`
  - cc_const_eval() does not handle AST_TERNARY or AST_COMMA operators. While these are less common in global initializers, C does allow `int x = (1 ? 2 : 3);` as a constant expression. Missing support means these silently become non-constant and zero-fill instead.
  - *fix:* Add cases for AST_TERNARY and AST_COMMA: fold the condition/left operand first, then fold the appropriate branch/final operand and return its value.

## sched-preempt (6)
- **[P1 c=0.95 fillable]** Missing time_slice reset for CRETURN process dispatch from IRQ (SCHED-FAIRNESS-0 path) — `kernel/core/sched/scheduler.c:3227-3234`
  - When schedule_from_irq() picks a RESUME_CRETURN process (a kernel thread, blocked-then-woken task, or recently-yielded process) via the SCHED-FAIRNESS-0 fairness fix, the code dispatches it via cooperative_switch_to() without initializing next->time_slice. If the process was previously yielded with time_slice > 0 (e.g., yielded after using 5 of 10 ticks), it resumes with that stale quantum value i
  - *fix:* Add `next->time_slice = priority_time_slice(next->priority);` immediately before or after `next->state = PROCESS_RUNNING;` at line 3227. This ensures the CRETURN process receives a fresh, priority-scaled quantum matching the behavior of RESUME_IRETQ paths.
- **[P1 c=0.85 fillable]** Race condition: CRETURN process dispatched to cooperative path without resuming quantum — `kernel/core/sched/scheduler.c:3185-3238`
  - In the SCHED-FAIRNESS-0 fix (lines 3185-3238), when schedule_from_irq() dispatches a RESUME_CRETURN successor via cooperative_switch_to(NULL, next), the code calls cooperative_switch_to() which does NOT return (line 3234 comment: 'DOES NOT RETURN'). If a CRETURN process has time_slice=0 or was never properly initialized, and the cooperative path eventually yields or blocks, the process will be re-
  - *fix:* Initialize next->time_slice = priority_time_slice(next->priority) before calling cooperative_switch_to() at line 3234. This ensures CRETURN processes receive a fair quantum before switching.
- **[P2 c=0.70 fillable]** Potential time_slice stale value for first-dispatch RESUME_IRETQ userspace processes — `kernel/core/sched/scheduler.c:3097-3182`
  - In the FIRST-DISPATCH block where schedule_from_irq() bootstraps a brand-new userspace process that was never run (context.rip == trampoline), the code synthesizes a fresh iretq frame and sets next->state = PROCESS_RUNNING at line 3152, but does not explicitly reset next->time_slice. While new processes happen to be created with time_slice = SCHED_QUANTUM_TICKS (10) in process_create(), a process_
  - *fix:* Add `next->time_slice = priority_time_slice(next->priority);` at line 3154 (after next->total_time++ and before the TSS setup). This ensures predictable, priority-aware quantum initialization for new userspace processes.
- **[P2 c=0.60 fillable]** Scheduler-in-switch race condition with GPF-001 reentrancy guard not cleared on IRQ path — `kernel/core/sched/scheduler.c:209, 2946-3302`
  - The global `scheduler_in_switch` reentrancy guard (line 209, declared as 'volatile int') is checked by timer_handler() to defer scheduling if a timer fires during a cooperative context switch (GPF-001). However, in PREEMPTIVE builds, schedule_from_irq() is the IRQ0 handler and never sets/clears this flag. If a nested timer interrupt fires while schedule_from_irq() is mid-switch (e.g., during CR3 l
  - *fix:* Either: (1) Add `scheduler_in_switch = 1;` near the start of schedule_from_irq() and `scheduler_in_switch = 0;` before each return path, OR (2) remove the cooperative-path guard entirely if preemption is enabled (since nested preemptions on the same CPU are impossible with IF=0 during iretq). Option 1 is safer for mixed-mode kernels.
- **[P3 c=0.40 fillable]** pit_tick() called before sleep_list_wake_due() may cause missed wakeups or timing skew — `kernel/core/sched/scheduler.c:2949-2957`
  - In schedule_from_irq(), pit_tick() is called at line 2949 to increment timer_ticks, then sleep_list_wake_due(timer_get_ticks()) is called at line 2957 to wake sleeping processes. The comment says 'Done before the quantum logic so a just-woken sleeper can be picked as the successor below', but this ordering is fragile: if pit_tick() increments the tick counter BEFORE the sleep wake scan, the scan s
  - *fix:* Add a comment clarifying the wake-deadline semantics, or swap the order to match the cooperative path intent: call sleep_list_wake_due(timer_get_ticks()) BEFORE pit_tick() so wake uses the OLD tick count, then increment. This prevents off-by-one wake timing.
- **[P3 c=0.30 fillable]** Missing validation that picked process is not NULL before use in RESUME_CRETURN dispatch — `kernel/core/sched/scheduler.c:3185-3238`
  - At line 3035, scheduler_pick_next() returns a process (guaranteed non-NULL per the function's contract). However, the SCHED-FAIRNESS-0 block (lines 3185-3238) assumes next != NULL without an explicit check before accessing next->resume_mode at line 3185. While the logic guarantees this (if next==NULL then the idle fallback is returned), a future refactor or a subtle bug in pick_next could make nex
  - *fix:* Add 'ASSERT_ALWAYS(next != NULL);' at line 3185 before the resume_mode check, or after line 3035. This documents the contract and catches regressions.

## mem-cow-fork (6)
- **[P1 c=0.95 fillable]** fork() does not inherit signal handlers from parent — `kernel/core/syscall/handlers.c:326-520`
  - sys_fork() creates a child process but does not copy the parent's signal handlers (sig_handlers[32]) or signal mask (sig_mask) to the child. POSIX requires these to be inherited. The child starts with all signal handlers zeroed (SIG_DFL) and no blocked signals, regardless of what the parent had configured. This breaks signal behavior for any forked child that relies on inherited signal handlers.
  - *fix:* After line 417 (VMA copy), add: for(int i=0;i<32;i++)child->sig_handlers[i]=parent->sig_handlers[i]; child->sig_mask=parent->sig_mask; child->sig_restorer=parent->sig_restorer;
- **[P1 c=0.92]** No exit_group implementation for multi-threaded process termination — `kernel/core/syscall/handlers.c:44-78`
  - POSIX specifies exit_group(2) which terminates all threads in a thread group when one thread calls exit(). Currently sys_exit() only marks the single calling thread/process as TERMINATED. In a multi-threaded process, calling exit() from one thread should kill all threads in the group, but the implementation does not do this. A thread can exit leaving sibling threads still running, or all threads c
  - *fix:* Implement SYS_EXIT_GROUP that walks the process table, finds all processes with tgid==current->tgid, marks them all TERMINATED, and wakes their parents. Alternatively, modify sys_exit to check if is_thread and if so, find all threads with the same tgid and terminate them collectively.
- **[P2 c=0.70 fillable]** CoW refcount ceiling at 255 owners silently drops 256th fork — `kernel/core/mem/cow.c:39-106`
  - The CoW refcount table uses uint8_t (0-255) to track 'extra owners' beyond the first. When a page is shared 256 times (refcount=255, meaning 256 total owners), the 256th fork cannot call cow_incref() because the check at line 99 rejects cow_table[pfn]>=255. The fork silently falls back to eager copy instead of the CoW optimization. While this is safe (correctness preserved), it's a silent degradat
  - *fix:* Add a kprintf log when cow_incref returns 0 due to saturation: 'CoW refcount ceiling hit at 256 owners of frame 0x%lx, falling back to eager copy for this page'. This surfaces the limitation to developers. Or increase the refcount table to uint16_t if the semantic allows (costs 512KB vs 256KB memory).
- **[P2 c=0.65 fillable]** Parent's TLB not flushed if all forks fail during fork_copy_user_pages — `kernel/core/syscall/handlers.c:256-282`
  - fork_copy_user_pages() may demote the parent's writable PTEs to RO+CoW in place (line 218) as it processes pages. If a later page fails to allocate or map (FORK#4 check at line 229), the function jumps to fail: (line 268). The fail path does flush the parent's TLB if parent_pte_changed (line 277), but only AFTER dropping paging_reset_target(). However, if fork fails before any demotions occur (e.g
  - *fix:* Track whether ANY PTE demotion happened anywhere in the walk (even if only on huge pages), not just in the 4KB path. Set parent_pte_changed=1 whenever demoting. Or, flush the parent's TLB unconditionally in the fail path before checking parent_pte_changed, since it's cheap and safe.
- **[P3 c=0.55 fillable]** VMA file_ptr not validated for lifetime safety during fork — `kernel/core/syscall/handlers.c:412-418`
  - fork() copies the parent's VMA list to the child via vma_add(). The VMA descriptor includes file_ptr (line 405 comment says 'file-backed VMAs share the immutable initrd-image file pointers'). However, there is no validation that file_ptr actually points to valid, immortal data. If file_ptr points to heap memory that could be freed, or to a file descriptor's buffer that could be closed, the child's
  - *fix:* Add a comment documenting the assumption that all file-backed VMAs must have file_ptr pointing to initrd data. Or, validate at vma_add time that file_ptr is within the initrd bounds. Or, refactor to store (inode_id, offset) instead of raw pointers.
- **[P3 c=0.45 fillable]** Child process inherits parent's pending signals (sig_pending) — `kernel/core/sched/process.c:224`
  - While not copying sig_handlers or sig_mask is a bug, the issue is worse: the child inherits whatever sig_pending (pending signal bits) the parent had when process_create memsets the PCB to zero. Pending signals should NOT carry over to the forked child. A parent with a pending SIGALRM that hasn't been delivered yet will not have that pending in the child—this is correct by accident via the memset.
  - *fix:* Explicitly document and test that sig_pending is correctly zeroed in the child (never inherited). Add a comment in process_create: 'Pending signals are NOT inherited; child starts with a clean sig_pending=0 via memset.'

## syscall-boundary (6)
- **[P1 c=0.95]** sys_map_file maps to fixed VA allowing cross-process disclosure — `kernel/core/syscall/handlers.c:1731-1742`
  - sys_map_file always maps files to fixed address 0x50000000. Multiple calls to map different files collide at the same VA, allowing a process to observe which files previous processes mapped by attempting mappings and observing at the fixed address. No per-AS VA allocation or collision detection.
  - *fix:* Allocate per-process unique VA ranges for mappings via the existing vmm_mmap_anon infrastructure (which uses per-AS bump allocators), or track mapped ranges per-AS to prevent collisions.
- **[P2 c=0.85 fillable]** sys_map_file lacks upper-bound overflow check on mapped VA range — `kernel/core/syscall/handlers.c:1726-1742`
  - When calculating pages and mapping (base_va=0x50000000 + pages*PAGE_SIZE), no validation that the final mapped range stays below USER_SPACE_END. A large file could map pages past the user-space boundary, potentially mapping into kernel-only regions or wrapping the address space.
  - *fix:* Add overflow check: reject files where 0x50000000 + (pages * PAGE_SIZE) > USER_SPACE_END before entering the mapping loop.
- **[P2 c=0.80 fillable]** validate_user_buffer incomplete - skips actual page accessibility check — `kernel/core/mem/vmm.c:78-81`
  - validate_user_buffer (used by syscalls) checks only address range bounds, not whether pages are actually mapped and accessible. Lines 78-79 have a TODO noting this gap. Callers may accept invalid pointers that would fault if dereferenced, allowing DoS or logic errors.
  - *fix:* Implement the TODO: call user_range_is_accessible(addr, size, false) before returning true, matching the fault-safety already done in copy_from_user (line 354).
- **[P2 c=0.75 fillable]** sys_ioctl passes unchecked user pointer to pty_ioctl without validation — `kernel/core/syscall/handlers.c:1796`
  - sys_ioctl passes argp (a user-space pointer) directly to pty_ioctl with no bounds checking or copy_to/from_user. pty_ioctl may dereference it unsafely, allowing ring-3 to trigger ring-0 #PF or read/write arbitrary user memory from kernel context.
  - *fix:* Validate argp via copy_to_user/copy_from_user for all ioctl requests before passing to pty_ioctl. Define ioctl-specific structs and marshal via safe copy functions.
- **[P3 c=0.65 fillable]** sys_map_file does not validate file_size against physical page boundaries — `kernel/core/syscall/handlers.c:1701-1726`
  - sys_map_file accepts file_size from inode->size with only a non-zero check (line 1704), then maps pages at phys_data + i*PAGE_SIZE without verifying the file_size fits within page-aligned boundaries. If file_size is not a multiple of PAGE_SIZE, the last mapped page may extend beyond the actual file data, potentially exposing stale kernel memory (page padding).
  - *fix:* Document the intended semantics: either round file_size up to next page boundary explicitly (padding is acceptable), or only map complete pages and return the page-rounded size to user instead of raw file_size. The latter requires truncating file_size in the copy_to_user call.
- **[P3 c=0.60 fillable]** copy_to_user CoW pre-resolution does not guard against loop wrap at page boundary — `kernel/core/mem/vmm.c:755-756`
  - When copy_to_user's writability check fails (read-only or CoW), lines 755-756 loop calling cow_handle_write on each page from lo to hi. The loop condition (pg <= hi && pg += 0x1000) does not guard against uint64_t overflow if the range extends to the top of address space, potentially skipping the final page's CoW resolution.
  - *fix:* Rewrite the loop to avoid wrap: use (pg < hi || (pg == hi && pg < dst_addr + n)) or calculate npages separately and iterate by count instead of address.

## signals (6)
- **[P1 c=0.95]** SMP g_sig_frame not per-CPU, breaks with multi-CPU signal handling — `kernel/core/signal/kill.c:292`
  - g_sig_frame is a single global pointer, acknowledged at line 291 comment as 'Single ring-3 CPU at a time on this path in the default build; SMP must make this per-CPU.' Under SMP_SCHED_DISPATCH, CPU1 can run ring-3 tasks. When CPU0 in syscall_dispatch writes g_sig_frame, then CPU1 calls sys_rt_sigreturn before CPU0 finishes delivery, sys_rt_sigreturn (line 464) reads stale/wrong g_sig_frame, corru
  - *fix:* Change g_sig_frame to a per-CPU array: sig_gpframe_t* g_sig_frame[SMP_SCHED_NCPU]; access via g_sig_frame[cpu_id()]. Update syscall.asm line 62 and 171 to index into the array, and sys_rt_sigreturn line 464 to read the per-CPU slot.
- **[P1 c=0.90 fillable]** Non-atomic sig_pending modification races in multi-CPU context — `kernel/core/signal/kill.c:243`
  - Line 243 uses plain |= to set sig_pending: 'target->sig_pending |= (1ull << sig)'. Under SMP, when CPU0 calls sys_kill() on a process running on CPU1, the RMW on sig_pending is not atomic. CPU1 simultaneously modifying sig_pending (line 359, 417, 452-454) creates a lost-update race. Process.c line 695 documents this: 'SMP must make the pending word atomic'. No spinlock guards cross-CPU sig_pending
  - *fix:* Protect sig_pending writes with __atomic operations or spinlock. Change line 243 to: __atomic_fetch_or(&target->sig_pending, (1ull << sig), __ATOMIC_SEQ_CST). Also audit and wrap lines 359, 417, 452-454 in local-only contexts (they are safe: called on current process only). For clarity, add comments.
- **[P2 c=0.88 fillable]** sigprocmask missing validation of 'how' parameter (silent fallthrough) — `kernel/core/signal/kill.c:452`
  - sys_rt_sigprocmask lines 452-454 use if/else-if/else on 'how', with default case (line 454) silently executing SETMASK for any invalid how value (e.g., how=999). POSIX requires EINVAL for invalid how. An attacker or buggy userspace passing how=3 will silently set sig_mask instead of rejecting the call, corrupting the process signal state.
  - *fix:* Add validation: after line 450, insert 'if (how > 2) return EINVAL;' to reject invalid how values before any state modification.
- **[P2 c=0.85]** Missing EINTR return for interrupted blocking syscalls — `kernel/core/signal/kill.c:237`
  - The signal design wakes blocked processes via wait_object_abort (lines 249, 331, 407) but does not return EINTR to the userspace syscall. POSIX specifies that a syscall interrupted by a signal should return -EINTR (not succeed with partial result). Currently, a blocked read/futex/waitpid that is awakened by a signal will continue waiting or incorrectly report success instead of EINTR. No mechanism
  - *fix:* Add an 'interrupted_by_signal' flag to process_t. Set it in signal_default_action (line 319) and wait_object_abort caller context. Have blocking syscalls check this flag at wakeup and return EINTR. Alternatively, use a blocked-syscall state field to track which syscall must return EINTR.
- **[P2 c=0.82 fillable]** sig_restorer default not initialized; can be NULL on first handler delivery — `kernel/core/signal/kill.c:369`
  - At line 369, sig_va_user(p->sig_restorer) is checked. But if sigaction was never called to set sig_restorer, it remains 0 from process_create's memset. The check 'sig_va_user(0)' returns false (line 301: 'va != 0 && va < SIG_USER_VA_MAX'), so signal_default_action is called instead of the handler. This means any catchable signal to a process that has not explicitly called sigaction(..., restorer=.
  - *fix:* Validate sig_restorer at sigaction time (line 422) and reject handlers (handler > 1) without a restorer: 'if (handler > 1 && !restorer) return EINVAL;'. Or provide a kernel-supplied default restorer and initialize sig_restorer to it at process_create.
- **[P2 c=0.78]** Missing check for g_sig_frame NULL in sys_rt_sigreturn on concurrent process teardown — `kernel/core/signal/kill.c:464`
  - At line 464, f = g_sig_frame is read. At line 467, NULL-check returns EFAULT. However, under SMP_SCHED_DISPATCH, a concurrent context (unrelated process's syscall on CPU1) could set g_sig_frame to a different value between the read at 464 and the access at 475+. This is a TOCTOU gap: the NULL-check passes but the frame is stale/wrong. Additionally, no check validates that f actually belongs to the
  - *fix:* Change architecture: instead of trusting g_sig_frame after the NULL check, re-derive the frame from rsp at sys_rt_sigreturn entry (the frame is always on the current process's kernel stack at the known offset). Or, store a per-process 'current_sig_frame' pointer that is local to each process, set by deliver_pending_signals, and read by sys_rt_sigreturn without a global lookup.

## poll-epoll (6)
- **[P1 c=0.95 fillable]** Timeout unit mismatch: milliseconds vs ticks in poll()/select()/epoll_wait() — `kernel/core/syscall/poll.c:111, 179`
  - sys_poll() and sys_select() add millisecond timeout directly to timer_get_ticks() return value (ticks). TIMER_FREQ=100 means 1ms ≈ 0.1 ticks. A 100ms timeout becomes ~1000ms because (current_ticks + 100ms_value) is compared against current_ticks, causing a 10x timeout inflation. Should use ms_to_ticks() from kernel/include/time.h.
  - *fix:* Line 111: uint64_t deadline = infinite ? 0 : timer_get_ticks() + ms_to_ticks((uint64_t)timeout); Line 179: uint64_t deadline = infinite ? 0 : timer_get_ticks() + ms_to_ticks((uint64_t)timeout); Also add #include "../../include/time.h"
- **[P1 c=0.95 fillable]** Timeout unit mismatch in epoll_wait() — `kernel/core/syscall/epoll.c:334`
  - sys_epoll_wait() adds timeout_ms (milliseconds) directly to timer_get_ticks() (ticks). Same unit mismatch as poll/select. A 100ms timeout waits ~1000ms. Should convert ms to ticks using ms_to_ticks() from kernel/include/time.h.
  - *fix:* Line 334: uint64_t deadline = infinite ? 0 : timer_get_ticks() + ms_to_ticks(timeout_ms); Also add #include "../../include/time.h"
- **[P2 c=0.98 fillable]** Dead code in sys_epoll_ctl after unreachable spinlock_release() — `kernel/core/syscall/epoll.c:309-310`
  - Lines 309-310 are unreachable. spinlock_acquire() at line 238 is followed by if-else chain where ALL branches return early (lines 240-307). Line 309 (spinlock_release) and 310 (return EINVAL) can never execute. The lock is released properly in each branch, so this is just dead code indicating a logic error in the control flow.
  - *fix:* Remove lines 309-310. All code paths in the if-else chain (EPOLL_CTL_ADD/MOD/DEL) already return after spin_unlock(), making the final spinlock_release() and return unreachable.
- **[P2 c=0.75]** No signal-interrupt handling (EINTR) in poll/select/epoll — `kernel/core/syscall/poll.c:114-133, 183-207`
  - sys_poll() and sys_select() have no check for signals that might interrupt the wait. If a signal handler is invoked during the blocking poll_sleep_slice(), the sleep completes but control returns to the loop without checking for signal delivery. POSIX-compliant poll/select should return -EINTR if a signal handler runs. This is a known architectural limitation (signals may not be fully integrated),
  - *fix:* Add signal delivery check after each poll_sleep_slice() call. If a signal is pending, check process_get_current()->pending_signals and return -EINTR. Requires integration with the signal delivery subsystem (current implementation status unknown).
- **[P3 c=0.70 fillable]** Inconsistent spinlock API usage in sys_epoll_ctl — `kernel/core/syscall/epoll.c:238, 244, 250, 264, 273, 279, 285, 293, 299, 305`
  - Line 238 uses spinlock_acquire(&ep->lock) but all returns use spin_unlock(&ep->lock). While spinlock_acquire and spin_lock are equivalent aliases (both defined in spinlock.h), using mismatched names creates confusion and suggests a potential error. The final unreachable line 309 uses spinlock_release() which is the correct complement for spinlock_acquire().
  - *fix:* Use consistent API: either change line 238 from spinlock_acquire() to spin_lock(), or all spin_unlock() to spinlock_release(). Recommend using spin_lock/spin_unlock for consistency with the rest of epoll.c (see lines 169, 175, 190, 202, 216, 344, 364, 369, 390, 394).
- **[P3 c=0.60 fillable]** File descriptor validation gap in epoll_ctl() before watch insertion — `kernel/core/syscall/epoll.c:227-274`
  - epoll_ctl(EPOLL_CTL_ADD) does not validate that the fd argument is a valid file descriptor before adding it to the watch list. The fd is only validated later when fd_poll_state() is called during epoll_wait(). While Linux epoll allows watching invalid fds (which immediately report POLLNVAL), an early validation would catch user errors faster. This is not strictly incorrect behavior but is less def
  - *fix:* Add validation in sys_epoll_ctl EPOLL_CTL_ADD path: call fd_poll_state(fd) before adding to watch list. If it returns POLLNVAL, return EBADF. This makes error detection immediate rather than deferred to epoll_wait().

## vfs (6)
- **[P1 c=0.95 fillable]** Missing SYS_LSEEK syscall wiring — `kernel/include/syscall.h:1-160`
  - vfs_lseek(fd, offset, whence) is fully implemented in kernel/fs/vfs.c (lines 992-1031) and correctly bounds-checks all SEEK_* modes, but there is NO syscall entry point wired into syscall_table. The syscall number SYS_LSEEK is not defined anywhere in the codebase. Userspace cannot invoke lseek() without this syscall; calls will fail silently or be interpreted as a different syscall number.
  - *fix:* Define SYS_LSEEK in kernel/include/syscall.h (suggest #define SYS_LSEEK 83 or similar unused number), add sys_lseek handler in kernel/core/syscall/handlers.c (thin wrapper calling vfs_lseek and marshalling errno), and register it in syscall_table in kernel/core/syscall/syscall.c alongside the other file I/O syscalls (SYS_READ, SYS_WRITE, SYS_OPEN, SYS_CLOSE).
- **[P1 c=0.92]** Relative symlink resolution not implemented — `kernel/fs/vfs.c:732-741`
  - vfs_path_lookup() explicitly rejects all relative symlinks by returning NULL (line 741). The code has a comment 'For simplicity, we'll just fail on relative symlinks for now (proper implementation would reconstruct the full path)'. This breaks POSIX semantics: symlinks like '/a -> ../b' will fail to resolve even in valid contexts. Any filesystem/initrd that includes relative symlinks will have tho
  - *fix:* Implement relative symlink resolution: when target[0] != '/', build the full path by concatenating the current directory inode with the relative target (similar to path reconstruction in vfs_rename/vfs_mkdir) and recurse into vfs_path_lookup_rec(). This requires tracking the current inode's path or maintaining a path string during the walk.
- **[P2 c=0.85 fillable]** Device node file operations not validated on dispatch — `kernel/fs/vfs.c:844-851`
  - vfs_open() assumes that device nodes (identified by VFS_TYPE_DEVICE + VFS_DEVNODE_MAGIC) store a valid vfs_devnode_t* in inode->private_data and that its ->fops pointer is always set (line 848). There is NO NULL check on either the devnode pointer or its fops field. If a device inode is malformed (private_data=NULL or devnode->fops=NULL), the code dereferences a NULL pointer or calls a NULL functi
  - *fix:* Add explicit validation: if ((inode->type & VFS_TYPE_DEVICE) && inode->private_data && *(uint32_t*)inode->private_data == VFS_DEVNODE_MAGIC) { vfs_devnode_t* dn = (vfs_devnode_t*)inode->private_data; if (dn->fops) { file->ops = dn->fops; } else { kfree(file); vfs_inode_put(inode); return VFS_ERR_NOSYS; } } else { file->ops = &ramfs_file_ops; } 
- **[P2 c=0.78 fillable]** Missing inode.type field validation in vfs_open — `kernel/fs/vfs.c:805-809`
  - vfs_open() checks if inode->type is a directory and rejects it (line 806). However, it does NOT validate that the inode is a regular file (VFS_TYPE_FILE) before proceeding to read/write. An inode with type=0 (uninitialized or device) could be opened for read/write, leading to incorrect I/O. The code assumes ops->read/ops->write exist, but a zero-type inode may have NULL ops or device-specific ops 
  - *fix:* Add explicit check: if ((inode->type & VFS_TYPE_FILE) == 0 && (inode->type & VFS_TYPE_DEVICE) == 0) { vfs_inode_put(inode); return VFS_ERR_INVAL; } before line 810 to reject symlinks, directories, and uninitialized inodes.
- **[P2 c=0.72 fillable]** Mount point matching fails for root mount with trailing slash — `kernel/fs/vfs.c:508-520`
  - vfs_find_mount() requires a path boundary after a matched mount prefix (e.g., '/' or next path separator) to avoid '/foo' matching paths like '/foobar'. However, the boundary check (line 514-516) allows mount->target ending in '/' to always match, but does NOT handle the case where mount->target='//' (double slash). More critically: if '/' is mounted normally, a path like '/foo' will correctly mat
  - *fix:* Normalize mount->target in vfs_mount() to remove trailing slashes (except for root '/'), or adjust path boundary logic in vfs_find_mount to handle all cases uniformly. Ensure that when a mount is found, the path offset past the mount prefix is calculated correctly (e.g., for '/' mount and path '/foo/bar', the relative path is '/foo/bar', not 'foo/bar').
- **[P2 c=0.68]** Incomplete '..' navigation at mount root lacks cross-mount handling — `kernel/fs/vfs.c:621-627, 657-670`
  - vfs_path_lookup() only tracks a single level of parent (line 627: 'parent = NULL' for above-root, and line 666: 'parent = NULL' after '..'). At a mount point's root, attempting '..' does NOT cross into the parent mount's root; it silently stays at the current mount root (line 668 is a no-op). In a real filesystem with multiple mounts (e.g., '/' and '/mnt/disk'), '/mnt/disk/../' should resolve to '
  - *fix:* When '..' is encountered at a mount root (parent == NULL), check if a parent mount exists above the current mount (via vfs_find_mount checking the parent directory path of the current mount->target), and if so, navigate into that mount's root. This requires cross-mount awareness during path traversal, similar to real kernel VFS implementations.

## diskfs (4)
- **[P1 c=0.95 fillable]** Block count validation allows out-of-bounds disk writes — `kernel/fs/diskfs.c:468`
  - dfs_mount_from_sb() checks sb->block_count > DFS_BITMAP_BITS (4096) but should check against DFS_BLOCK_COUNT (3584), the actual disk capacity. A corrupted superblock claiming 4000 blocks would pass validation. Writing to such blocks would compute LBA = 4096 + 4000*8 = 36088, exceeding the 32768-sector disk, causing out-of-bounds writes to undefined sectors.
  - *fix:* Line 468: Change 'sb->block_count > DFS_BITMAP_BITS' to 'sb->block_count > DFS_BLOCK_COUNT' and add explicit check 'sb->block_count == 0 ||' to ensure count is in valid range [1, 3584].
- **[P2 c=0.90 fillable]** Misleading 'mounted' message when filesystem fails to format — `kernel/fs/diskfs.c:831-834`
  - If diskfs_format() fails and the superblock write succeeds, diskfs_init() still prints '[DISKFS] mounted: boot #...' message and sets g_diskfs_ready=true, even though g_fs.mounted is false (never set). This misleads about filesystem availability. The message should only be printed when g_fs.mounted is actually true.
  - *fix:* After line 823 mount check, check g_fs.mounted before printing the success message at line 831. Only set g_diskfs_ready and print 'mounted' message if g_fs.mounted is true.
- **[P2 c=0.85 fillable]** Block free error silently ignored during unlink — `kernel/fs/diskfs.c:279`
  - dfs_block_free() calls dfs_flush_bitmap() but ignores its return value (line 279). If bitmap flush fails during file deletion, the block's allocation bit is cleared in memory but not on disk. On reboot, the block shows as allocated. Subsequent writes may allocate the same block to a different file, causing two inodes to share data blocks.
  - *fix:* Store return value of dfs_flush_bitmap() and propagate error up the call chain. dfs_free_inode_blocks() should return int error status, diskfs_unlink() should check it, and fail the entire unlink if bitmap flush fails.
- **[P3 c=0.60 fillable]** Fresh format filesystem not mounted after diskfs_init success — `kernel/fs/diskfs.c:823-827`
  - After successful fresh format, diskfs_format() sets g_fs.mounted=true and initializes g_fs layout fields. However, dfs_mount_from_sb() is explicitly skipped for fresh_format (only called for existing fs). While diskfs_format() does set up the necessary state, the architectural inconsistency is confusing: fresh vs existing filesystems take different paths through mount logic.
  - *fix:* Clarify intent with a comment explaining why fresh and existing filesystems diverge at line 822. Consider unifying to always call dfs_mount_from_sb() or explicitly document the design rationale.

## wifi-sim (6)
- **[P1 c=0.95]** Status syscall does not populate BSSID and SSID fields — `/c/Users/wilde/Desktop/Kernel/kernel/net/wlansyscall.c:110-114`
  - sys_wlan_status() only fills state and rssi (lines 112-113) but the uapi_wlan_status_t struct has bssid[6] and ssid[32] fields that are never populated. These fields remain zeroed after memset on line 111. This breaks status reporting completeness and applications cannot determine which AP is actually connected to.
  - *fix:* After line 113, retrieve the connected BSSID and SSID from the wifi driver (or simulated state) and populate out.bssid and out.ssid (and out.ssid_len). The driver should provide these via a get_status extended signature or separate calls.
- **[P1 c=0.95 fillable]** WiFi simulator does not track connected AP's BSSID — `/c/Users/wilde/Desktop/Kernel/kernel/drivers/net/wireless/sim/wifisim.c:38-42, 72-98`
  - The wifisim backend maintains g_sim_state, g_sim_ssid, and g_sim_rssi but has no g_sim_bssid storage. When sim_connect() is called, it does not save which AP's BSSID was chosen. The sim_get_status() function cannot return the connected BSSID, leaving it at zeros. This prevents the control plane from knowing which AP is connected.
  - *fix:* Add a static uint8_t g_sim_bssid[6] field (line 42). In sim_connect() (line 72), extract the BSSID from the bss parameter and store it in g_sim_bssid. Optionally extend wifi_ops.get_status() signature to accept BSSID/SSID output pointers, or create new callbacks.
- **[P1 c=0.85 fillable]** No GTK (Group Temporal Key) installation in supplicant — `/c/Users/wilde/Desktop/Kernel/userspace/apps/wpasupp/wpasupp.c:276-297`
  - The wpasupp only installs the PTK (Pairwise Temporal Key, extracted from PTK[32..48] as TK). WPA2 and WPA3 protocols also require installation of a GTK (for multicast/broadcast decryption). The GTK is delivered by the AP in EAPOL message 3 of the 4-way handshake. Since wpasupp uses demo nonces and does not exchange real EAPOL frames, the GTK is never derived or installed. This is a protocol-level 
  - *fix:* Implement a complete 4-way handshake simulation: (1) Pre-compute or derive a demo GTK; (2) Call SYS_WLAN_SET_KEY with pairwise=0 (group key) to install it after the PTK. Alternatively, document this as M2+ future work and ensure the M1 simulator note explains GTK is deferred.
- **[P2 c=0.90 fillable]** SAE scalar randomness uses hardcoded demo values, not CSPRNG — `/c/Users/wilde/Desktop/Kernel/userspace/apps/wpasupp/wpasupp.c:216-221`
  - In WPA3 SAE exchange, the random scalars (ra, ma, rb, mb) are deterministically filled with sequential byte patterns (0x11..0x42, etc., lines 219-220) instead of being drawn from a cryptographic random source. This is documented as a demo-mode design (comment line 214: 'demo randoms'), but in production code this would be a severe cryptographic weakness—nonce reuse is a key attack vector against S
  - *fix:* In real deployment, replace lines 218-221 with calls to a CSPRNG (e.g., getrandom() / SYS_GETRANDOM). For M1 simulation, add a prominent comment and perhaps a #ifdef WIFI_SIM guard. Document in the README that M2 must use real random scalars.
- **[P2 c=0.65]** No IEEE 802.11 Annex J.10 published KAT vectors for SAE — `/c/Users/wilde/Desktop/Kernel/userspace/lib/crypto/sae.c:623-648`
  - The SAE implementation's sae_selftest() uses only internal self-consistency tests (T1-T4: PWE determinism, two-party exchange, negative tests, KDF cross-check) and a hardcoded regression vector ('mekmitasdigoat' at line 651). The published IEEE 802.11-2020 Annex J.10 test vector is not included because the full byte sequences were not accessible at authoring (comment line 626-627). A hardcoded IEE
  - *fix:* Once an authoritative IEEE Annex J.10 vector is obtained (or derived from an official reference like hostap), add a hardcoded test case that validates PWE x/y and final PMK against the published values. This can coexist with the current self-consistency tests.
- **[P3 c=0.50 fillable]** SAE confirm exchange uses 'constant-time-ish' not true constant-time comparison — `/c/Users/wilde/Desktop/Kernel/userspace/lib/crypto/sae.c:588-593`
  - The sae_check_confirm() function performs a timing-resistant XOR-accumulation compare (lines 589-590: 'diff |= ...'), which the comment (line 588) explicitly calls 'Constant-time-ish' rather than truly constant-time. The implementation relies on compiler behavior (no optimization of the XOR loop) rather than hardware guarantees. An aggressive compiler or CPU-level timing side-channels could leak c
  - *fix:* Adopt a formally verified constant-time compare (e.g., from a crypto library like libsodium crypto_verify_32) or use inline asm with explicit cycle counts. For M1 simulation this is not urgent, but flag for M2 hardening.

## audio-hda (6)
- **[P1 c=0.95 fillable]** Stream reset bit wait logic inverted (hda_stream_setup) — `kernel/drivers/hda_stream.c:228-236`
  - The stream reset acknowledge wait (line 231) has inverted logic. After writing SRST=1, hardware should clear the bit to 0 when reset completes. The code waits 'if (bit & 1) break' meaning it breaks when the bit is SET, but should wait for the bit to CLEAR (0). This causes immediate return instead of actual reset completion polling, potentially leaving the stream in an inconsistent state before con
  - *fix:* Change line 231 from 'if (hda_sd_read32(ctrl, sd, HDA_SD_CTL) & 0x00000001)' to 'if (!(hda_sd_read32(ctrl, sd, HDA_SD_CTL) & 0x00000001))' to correctly wait for hardware to clear the SRST bit.
- **[P1 c=0.92 fillable]** hlt() in bare loop can hang if called with IF=0 (hda_wav.c) — `kernel/drivers/hda_wav.c:245-247`
  - The hda_play_wav() function uses a bare 'for(i < duration_ms) { hlt(); }' loop to wait for playback. If called from syscall context (SYS_BEEP) where IF=0, the hlt() instruction will not wake on any interrupt, and the loop will not advance duration_ms (a count, not ticks). This creates an indefinite hang since hlt() requires IF=1 to resume on interrupt, and the loop counter never decrements because
  - *fix:* Replace the hlt() loop with hda_msleep(duration_ms) which correctly handles both boot-phase (IF=0, no process) and syscall contexts (IF=0, process exists) via timer_sleep() or yield-with-cap, matching the pattern used in hda_play_wav() for audio_play_tone().
- **[P2 c=0.80]** HDA_ENABLE compile gate prevents audio in all standard builds — `kernel/kernel.c:931-961`
  - Audio initialization is gated behind #ifdef HDA_ENABLE (line 931), which defaults OFF. The code comment explains this is because real T410 hardware hangs during reset (line 939), but QEMU works fine. Without HDA_ENABLE=1 at compile time, hda_init() is never called, so the HDA controller is never discovered, enumerated, or configured. This means audio is effectively disabled in all builds unless ex
  - *fix:* Provide a runtime control mechanism (e.g., a boot parameter or sysctl) to enable/disable HDA, with safe defaults (off on real hardware, on in QEMU/test VMs). Alternatively, detect the platform at runtime and only skip HDA on known-bad hardware.
- **[P2 c=0.75]** No mixer (HDA_WIDGET_AUDIO_MIXER) widget support — `kernel/drivers/hda.c:714-854`
  - The codec setup code searches for and configures DAC and pin widgets, but never searches for MIXER widgets (type 0x2). Some HDA codecs have a mixer node between the DAC and output pin. If the codec topology is DAC->MIXER->PIN instead of DAC->PIN, the current hardwired DAC->PIN connection will silently fail. The code will write directly to the PIN's connection select register with DAC_NID, but the 
  - *fix:* Add widget topology discovery: after finding DAC and PIN, search for a MIXER widget and determine the correct connection chain (DAC->MIXER->PIN or DAC->PIN). Update hda_codec_setup_output() to connect pin to either the mixer (if present) or directly to DAC.
- **[P2 c=0.70]** Single-shot refill logic missing for streaming playback — `kernel/drivers/hda_stream.c:593-601`
  - The hda_irq_handler() increments g_hda_bcis counter when a Buffer Completion (BCIS) interrupt fires, proving DMA movement. However, there is no callback or refill mechanism to handle the user refilling the buffer when BCIS fires for streaming playback longer than DMA_BUFFER_SIZE (64 KB). The comment at line 599 acknowledges this ('Could also trigger a refill callback here for streaming playback') 
  - *fix:* Implement a refill callback in the interrupt handler or expose a poll function that userspace/kernel can call to refill at BCIS boundaries. Alternatively, document that playback is limited to buffer_size and require chunked playback for longer audio.
- **[P3 c=0.85 fillable]** Unused function stub (hda_wait_for_response) — `kernel/drivers/hda.c:18, 432-452`
  - hda_wait_for_response() is declared (line 18) and defined (lines 432-452) but never called. The codebase uses hda_immediate_command() (Immediate Command Interface) instead of the CORB/RIRB DMA path for which this function was designed. This is a legacy stub that can be safely removed to reduce clutter.
  - *fix:* Remove the declaration at line 18 and the definition at lines 432-452. The immediate command path via hda_immediate_command() is the working verb transport mechanism.

## pci (6)
- **[P1 c=0.95 fillable]** Missing pci_enable_io_space() kernel API function — `kernel/drivers/pci.c:343-363`
  - The kernel provides pci_enable_bus_master() and pci_enable_memory_space() but is missing pci_enable_io_space(). I/O-space devices (RTL8139, UHCI) currently work around this by directly reading/writing the COMMAND register via pci_config_read/write_word(), bypassing the kernel API abstraction and creating code duplication and inconsistency.
  - *fix:* Add a pci_enable_io_space() function (symmetric to pci_enable_memory_space) that ORs PCI_COMMAND_IO_SPACE (0x0001) into the COMMAND register at offset 0x04, mirroring the pattern of lines 354-362.
- **[P1 c=0.92 fillable]** Silent device table overflow at enumeration limit (PCI_MAX_DEVICES=64) — `kernel/drivers/pci.c:226-227`
  - When g_pci_device_count reaches PCI_MAX_DEVICES (64), pci_check_function() silently returns without logging or error reporting. Systems with more than 64 PCI devices (real T410s with bridges, or large QEMU setups) will have devices silently dropped from enumeration, and drivers cannot detect or diagnose the loss. The full scan continues, but enumeration is incomplete and the kernel operator is una
  - *fix:* Log a warning at line 227 before returning: kprintf("[PCI] Warning: device table overflow at %u devices; additional devices ignored\n", PCI_MAX_DEVICES);
- **[P2 c=0.90]** No secondary/subordinate bus enumeration for PCI bridges — `kernel/drivers/pci.c:259-288`
  - pci_init() brute-force scans bus 0..255 independently without recursive scanning of secondary buses behind PCI-to-PCI bridges (header type 0x01). Devices on secondary buses (e.g., PCIe slot expansion, CardBus, hot-plug docks) are never enumerated. Real hardware like the T410 has PCI Express root ports and bridges; their secondary buses contain real devices but are unreachable. This limits function
  - *fix:* After reading a device with header type 0x01 (bridge), extract the secondary_bus and subordinate_bus fields (offsets 0x19 and 0x1A), then recursively call pci_init_bus(secondary_bus, subordinate_bus) to enumerate all secondary buses. Wire the recursion into the main enumeration loop or post-process the bridge list after initial enumeration.
- **[P2 c=0.88]** pci_find_class() and pci_find_device() return only first match, blocking multi-device support — `kernel/drivers/pci.c:294-314`
  - Both pci_find_device() (lines 294-302) and pci_find_class() (lines 304-314) return immediately on the first match and return NULL if no match is found. Systems with multiple Ethernet NICs, USB controllers, or other devices of the same class cannot use the kernel PCI API to enumerate or select among them. Multi-NIC failover, load balancing, or device selection patterns are impossible. Drivers work 
  - *fix:* Add pci_find_device_by_index(vendor_id, device_id, index) and pci_find_class_by_index(class, subclass, prog_if, index) that accept an enumeration index and return the Nth matching device, or NULL if index >= count. Alternatively, add pci_get_next_device(prev_dev, vendor_id, device_id) for iterative enumeration.
- **[P2 c=0.78 fillable]** Interrupt_line values (0x00 / 0xFF) not validated before driver use — `kernel/drivers/pci.c:153-156`
  - pci_read_function() reads interrupt_line and interrupt_pin without validation. In systems without proper ACPI/BIOS IRQ routing, these fields may contain sentinel values (0x00 = unknown, 0xFF = no interrupt) or invalid IRQ numbers. The HDA driver assumes irq >= 16 is safe to ignore (line 218 of hda.c), but other drivers may register handlers on bogus IRQ lines. On systems with pure APIC (no legacy 
  - *fix:* Document in pci_read_function() that interrupt_line=0x00 and 0xFF are undefined values and drivers must validate before use. Optionally add a pci_interrupt_valid(line) helper that returns false for sentinel values. Systems with ACPI should populate interrupt_line via the DSDT instead of relying on the BIOS-filled config register.
- **[P3 c=0.72]** 64-bit BAR handling lacks validation of BAR size and alignment — `kernel/drivers/pci.c:174-207`
  - pci_read_function() reads raw BAR values but does not validate that the addresses are safe (not in reserved ranges like 0xFEE00000 for APIC MMIO) or that the BAR size is reasonable. The code correctly guards against reading past num_bars (line 194) for 64-bit BARs, but does not probe BAR sizes by writing 0xFFFFFFFF and reading back to detect decode width. Large BAR mappings (e.g., BAR0 on NVIDIA G
  - *fix:* After reading a memory BAR, optionally validate against the platform's MMIO hole (typically 0xE0000000..0xFEE00000 on x86-64). For safety-critical systems, probe the BAR size (write 0xFFFFFFFF, read back, mask, and check against max addressable size) and warn if it exceeds a reasonable bound (e.g., 1GB for a single BAR).

## comp-damage (6)
- **[P1 c=0.95 fillable]** PH_SNAPPING animation lacks full-damage cooldown, risking scissor clipping — `userspace/compositor/compositor_m8.c:5070`
  - Window snap animations (left/right/maximize) call anim_begin(PH_SNAPPING) which marks the frame dirty, but do not set g_full_damage_cooldown or call damage_add_full(). If a concurrent client commits a small damage rect in the same frame, the damage scissor narrows to that rect. The snapping window's animated footprint may fall outside the narrow scissor, leaving stale pixels at the old position - 
  - *fix:* Add g_full_damage_cooldown = FULL_DAMAGE_COOLDOWN_FRAMES; and damage_add_full(); after anim_begin() in start_geom_tween(), matching the pattern used for CREATE (line 4235-4236) and CLOSE (line 4362-4363).
- **[P1 c=0.95 fillable]** PH_MINIMIZING animation lacks full-damage cooldown — `userspace/compositor/compositor_m8.c:5040`
  - Window minimize animations call anim_begin(PH_MINIMIZING) which marks dirty but do not set cooldown or call damage_add_full(). When a window animates toward the taskbar, its old screen position needs to be repainted to reveal what's behind. If a concurrent client commit narrows the scissor before the old footprint is covered, ghosting can occur. Same threat model as SNAPPING gap.
  - *fix:* Add g_full_damage_cooldown = FULL_DAMAGE_COOLDOWN_FRAMES; and damage_add_full(); to begin_minimize() after anim_begin().
- **[P1 c=0.95 fillable]** PH_RESTORING animation lacks full-damage cooldown — `userspace/compositor/compositor_m8.c:5051`
  - Window restore (un-minimize) animations call anim_begin(PH_RESTORING) without cooldown or full-damage flag. When a window animates back from the taskbar, the taskbar slot and window's new position need full-scene recomposite. Concurrent client commits could narrow the scissor, clipping the animated restore motion, leaving ghosting.
  - *fix:* Add g_full_damage_cooldown = FULL_DAMAGE_COOLDOWN_FRAMES; and damage_add_full(); to begin_restore() after anim_begin().
- **[P2 c=0.75 fillable]** Toast fade-in/out lacks damage accumulation — `userspace/compositor/compositor_m8.c:2744`
  - toast_show() calls mark_dirty() but does NOT call damage_add() for the toast rectangle. While anim_tick() keeps the frame dirty (line 1654), the damage accumulator g_dmg_any remains 0, so the scissor is never narrowed. However, if toast rendering is re-enabled in the future and a concurrent client commits, the narrow scissor could clip the toast's old or new position during fade-in/out, leaving st
  - *fix:* In toast_show(), after setting g_toast_text and g_toast_dur_ms, compute the toast bounding box (tx, ty, tw, th using same logic as render_toast lines 2766-2773) and call damage_add(tx, ty, tx+tw, ty+th) to register it. Or simpler: call damage_add_full() to be conservative (toast is usually small/top-right anyway).
- **[P2 c=0.70 fillable]** Window fade_alpha animation lacks damage after PH_OPENING phase ends — `userspace/compositor/compositor_m8.c:1652`
  - When a new window opens, PH_OPENING animation and fade_alpha fade-in both start. The CREATE handler sets g_full_damage_cooldown=3 to protect both animations. However, fade_alpha (FADE_IN_MS=150ms) might outlast PH_OPENING (~220ms typical). Once phase becomes PH_NONE but fade_alpha < 255, anim_tick() marks dirty (line 1652) but does NOT call damage_add(). If a client commits small damage in this wi
  - *fix:* In anim_tick(), when checking fade_alpha < 255 at line 1652, also set g_full_damage_cooldown or call damage_add() for each fading window's bounding box to ensure its footprint is tracked in the damage accumulator.
- **[P3 c=0.60 fillable]** Damage accumulator lacks upper-bound clamping to screen size — `userspace/compositor/compositor_m8.c:306-320`
  - damage_add() clamps x0/y0 to 0 but does NOT clamp x1/y1 to g_fb_w/g_fb_h. Over time, the accumulator can grow to 0x7FFF (used in damage_add_full line 322). While each rasterizer (fill_rect, blend_rect, etc.) clamps the scissor against buffer bounds, this loose accumulation is semantically unclear and could mask bugs if the clamping in rasterizers is ever missed or bypassed. Not a present correctne
  - *fix:* In damage_add(), add clamping for x1/y1: if (x1 > (int32_t)g_fb_w) x1 = (int32_t)g_fb_w; if (y1 > (int32_t)g_fb_h) y1 = (int32_t)g_fb_h;  after the union logic to keep the accumulator within screen bounds.

## comp-render (6)
- **[P1 c=0.95 fillable]** cz_text Y scissor bypass — text renders outside damage band — `userspace/compositor/compositor_m8.c:1205-1214`
  - cz_text() applies only X-axis damage scissor clipping (g_scis_x0/x1) but completely ignores Y-axis bounds (g_scis_y0/y1). When the damage scissor is narrowed to exclude the titlebar region, window title glyphs still render outside the clipped band, violating the scissor invariant. This can cause coherency issues and defeats the damage-optimization purpose.
  - *fix:* Pass Y scissor bounds to font2_draw_cell_clip: extract cy0/cy1 from g_scis_y0/y1 (clamped to [0,bh)) and pass them to font2_draw_cell_clip (which already supports Y clipping via pixel-level bounds checks in its inner loop). Alternatively, add explicit Y range validation in cz_text before calling font2_draw_cell_clip.
- **[P1 c=0.85 fillable]** cz_text called in panel/dock without Y clipping context — `userspace/compositor/compositor_m8.c:1780, 1977, 2112-2117, 2152, 2563, 2583, 2597, 2606, 2626, 2638, 2670, 2695, 2810, 3211, 3520, 3531, 3662, 3670, 3760-3764, 4686`
  - cz_text is called 20+ times to render text in windows, panels, docks, and overlays without passing Y clip bounds. While font2_draw_cell_clip internally bounds-checks each pixel's Y coordinate against maxh, it does not know about the semantically important chrome bands (PANEL_H, DOCK_H, work area). If the damage scissor's Y range is set to exclude a region, cz_text will still render glyphs in that 
  - *fix:* Modify cz_text signature to accept optional cy0/cy1 parameters (or read from globals like other rasterizers do with g_scis_*). Pass these to font2_draw_cell_clip. For calls in window rendering, pass clip_y0/clip_y1 (already computed). For calls in chrome rendering, pass PANEL_H/DOCK_H or full-screen bounds. This ensures Y scissor enforcement is consistent across all text rendering.
- **[P2 c=0.92 fillable]** damage_add() unclamped max bounds — rect extends past screen — `userspace/compositor/compositor_m8.c:306-319`
  - damage_add() clamps the min corner (x0,y0) to [0, screen) but does NOT clamp the max corner (x1,y1). When damage_add_full() calls damage_add(0, 0, 0x7FFF, 0x7FFF), the rect extends to 0x7FFF (32767) instead of the actual screen dimensions (e.g. 1920x1080). This unclamped rect is then assigned to the global scissor (g_scis_x1/y1) without further validation, causing rasterizers to clamp to invalid b
  - *fix:* Clamp x1 and y1 in damage_add() to the actual screen dimensions: x1 = (x1 > g_fb_w) ? g_fb_w : x1; y1 = (y1 > g_fb_h) ? g_fb_h : y1; OR clamp at scissor assignment time before compositing (line 6043-6044).
- **[P2 c=0.88]** present_diff full-frame scan ignores damage scissor — `userspace/compositor/compositor_m8.c:3856-3874`
  - present_diff(back, prev) is intended to optimize framebuffer writes by scanning only the dirty region. However, PHASE 1 (lines 3862-3872) always scans the entire back/prev buffers from y=0 to y=h and x=0 to x=w, regardless of the damage scissor (g_scis_*) that was set in composite(). This defeats the primary optimization: on a static desktop with only a small window changing, present_diff still pa
  - *fix:* In PHASE 1, clamp the scan bounds to the damage scissor: start y from max(0, g_scis_y0), end at min(h, g_scis_y1), and similarly for x. This requires present_diff to take g_scis_* as parameters OR read them directly if they are still live at call time. Verify scissor is correctly set before present_diff is called (line 6074).
- **[P2 c=0.72 fillable]** blit_surface_clip pre-computed hx0/hx1 may exceed source bounds — `userspace/compositor/compositor_m8.c:662-688`
  - blit_surface_clip pre-computes the horizontal visible span (hx0, hx1) at lines 669-673 for both screen and scissor bounds. However, the calculation `int32_t hx0 = dx < 0 ? -dx : 0` and bounds clamping may produce hx0 or hx1 values > sw (source width), which are not validated. Later at line 685, the loop `for (int32_t sx = hx0; sx < hx1; sx++)` will access srow[sx] where sx could be >= sw. While su
  - *fix:* Clamp hx1 to sw: if (hx1 > (int32_t)sw) hx1 = (int32_t)sw; immediately after line 673. Also add a check `if (hx0 >= hx1) return;` before the loop (already present at line 675, so this is a non-issue if hx0/hx1 are derived correctly).
- **[P3 c=0.65 fillable]** Damage scissor initialization uses fixed magic value 0x7FFF — `userspace/compositor/compositor_m8.c:281-282, 298-301`
  - The global scissor is pre-initialized to (0, 0, 0x7FFF, 0x7FFF) and reset to this value in scissor_reset_full() to represent 'full screen'. However, 0x7FFF (32767) is a fixed magic constant that may not match the actual framebuffer dimensions once g_fb_w/g_fb_h are known (e.g., 1920x1200 or 2560x1440 on modern displays). While secondary bounds checks in each rasterizer prevent out-of-bounds memory
  - *fix:* After g_fb_w/g_fb_h are set in _start, initialize the scissor to (0, 0, g_fb_w, g_fb_h) instead of using the fixed 0x7FFF constant. Update scissor_reset_full() to use these dimensions.

## libc-sig-time (6)
- **[P1 c=0.95 fillable]** sigprocmask() is a no-op stub — `userspace/libc/signal.c:211-222`
  - sigprocmask() does not wire to the kernel SYS_RT_SIGPROCMASK (108) syscall. It returns success but never actually blocks/unblocks signals. The kernel implements the real handler at kernel/core/signal/kill.c:439-457. Any process that calls sigprocmask to mask signals will find signals still delivered despite the mask.
  - *fix:* Replace stub with syscall to SYS_RT_SIGPROCMASK: call __sig_sc(SYS_RT_SIGPROCMASK, how, (long)set, (long)oldset) and return the result.
- **[P1 c=0.95 fillable]** sigpending() is a no-op stub — `userspace/libc/signal.c:297-305`
  - sigpending() always returns 0 (no pending signals) instead of querying the kernel via SYS_SIGPENDING (110). The kernel implements this at kernel/core/signal/kill.c:487-492, returning p->sig_pending. Code that needs to detect pending signals will be blind to them.
  - *fix:* Replace stub with syscall to SYS_SIGPENDING: call __sig_sc(SYS_SIGPENDING, 0, 0, 0) to fetch p->sig_pending, copy it to *set, and return 0 on success.
- **[P1 c=0.95 fillable]** raise() calls handlers directly instead of via kernel signals — `userspace/libc/signal.c:135-151`
  - raise(sig) consults the userspace signal_handlers[] table and invokes the handler synchronously. It does NOT call the kernel via kill(getpid(), sig), so:  (1) signal masks are never checked — masked signals are still delivered; (2) default actions (SIGKILL, SIGTERM, etc.) are never applied — raise(SIGKILL) just calls signal_handlers[SIGKILL] instead of terminating; (3) the kernel's deliver_pending
  - *fix:* Replace raise() implementation to call kill(getpid(), sig) which already exists in syscall.c and routes through the kernel signal machinery.
- **[P2 c=0.90 fillable]** nanosleep() loses sub-second precision — `userspace/libc/time.c:486-507`
  - nanosleep() truncates to whole seconds: it reads only tv_sec and rounds up only if tv_nsec >= 500ms. Requests like nanosleep({0, 1000000}) (1ms) are truncated to 0ms, and nanosleep({0, 100000000}) (100ms) is rounded UP to 1 second. This breaks timing-sensitive code. The kernel has SYS_GET_TICKS_MS (40) supporting millisecond precision; nanosleep should use that.
  - *fix:* Convert tv_sec to milliseconds + (tv_nsec / 1000000), call sleep(ms) converted to seconds via SYS_SLEEP, or use a dedicated nanosleep syscall. Set rem to zero only if the sleep completed (return 0); on signal interruption, set rem to the remaining time.
- **[P2 c=0.85]** lseek() is a stub returning -1 — `userspace/libc/syscall.c:81-88`
  - lseek() always returns -1. The kernel has vfs_lseek() (kernel/fs/vfs.c:992) that works, but there is no SYS_LSEEK syscall number defined or registered. Any file-seeking code (e.g., reading a specific offset in a file) will fail.
  - *fix:* Add SYS_LSEEK syscall number to kernel/include/syscall.h (e.g., 130 or next free), implement sys_lseek handler in kernel/core/syscall/handlers.c, register it in syscall_table, and wire userspace lseek() to call syscall6(SYS_LSEEK, fd, offset, whence, 0, 0, 0).
- **[P3 c=0.70 fillable]** malloc tcache comment mentions future per-thread support without sync primitive design — `userspace/libc/malloc.c:92-96`
  - The tcache comment states 'NOTE: AutomationOS does not yet have userspace threads or TLS infrastructure... this is currently a **global** tcache.' The comment says this 'can be upgraded to per-thread' when threading is added, but there is no design for synchronization. Once userspace threading (SYS_THREAD_CREATE exists at kernel/core/syscall/handlers.c) is in use, concurrent tcache access will cor
  - *fix:* Document the required synchronization strategy for multi-threaded environments: either (1) use futex-based lock for the global tcache, (2) implement per-thread tcache via __thread + TLS, or (3) use lock-free data structures. Add a TODO comment referencing this design.

## agent-rail (6)
- **[P1 c=0.95 fillable]** Missing O_NOFOLLOW on file operations in tool_* implementations — `userspace/apps/tool_write/tool_write.c:230`
  - tool_write opens files with O_WRONLY|O_CREAT|O_TRUNC but does not use O_NOFOLLOW. A symlink in /tmp or /home could redirect writes to protected system files, bypassing the path_write_allowed() check. The gate checks the ARGUMENT path, not the final resolved target. This affects tool_write, tool_rm, tool_rollback, tool_exec, and tool_cc - any tool that opens/mutates files.
  - *fix:* Add O_NOFOLLOW (0x100 per kernel/include/vfs.h) to open flags for file operations. Change line 230 from `sc(SYS_OPEN,(long)path,O_WRONLY|O_CREAT|O_TRUNC,0)` to `sc(SYS_OPEN,(long)path,O_WRONLY|O_CREAT|O_TRUNC|0x100,0)`. Apply same fix to tool_rm.c:102, tool_rollback.c, tool_exec.c, tool_cc.c.
- **[P2 c=0.85]** Ledger persistence is in-memory ramfs only (/var/log/ai/agent.log) — `userspace/apps/agentd/agentd.c:238, 287-291`
  - The agent_ledger writes to /var/log/ai/agent.log, but /var is ramfs (as noted in ledgerver.c line 10: '/var is ramfs => in-session only'). This means the ledger is lost on reboot. For a truly auditable system, the ledger must persist to non-volatile storage. The adversarial audit trail is critical for post-incident forensics, and an in-memory ledger defeats that goal. ledgerver itself is only a va
  - *fix:* Change AGENT_LEDGER_PATH to persist to stable storage (e.g., /home/agent.log or /boot/agent.log, ensuring write access). Alternatively, implement a syscall to flush the ledger to durable storage (e.g., a kernel facility that writes to a protected block device or network). Coordinate with the kernel to ensure /var/log/ai persists if it's moved out of ramfs for this use case. The fix requires unders
- **[P2 c=0.80 fillable]** JSON parser in agentd accepts unclosed strings and malformed escape sequences — `userspace/apps/agentd/agentd.c:331-341`
  - The json_str() parser on line 334 iterates `while(*p && *p!='\'"')` without strictly validating the JSON format. Specifically: (1) an escape sequence on line 336 with `switch(e)` does not handle all JSON escapes uniformly (e.g., '\u' unicode escapes are not parsed, and invalid escape sequences like '\x' or '\q' are silently accepted as literal bytes); (2) the loop can exit with *p==0 (end of strin
  - *fix:* Fix the parser: (1) strictly reject unrecognized escape sequences by returning 0 in the switch default case instead of accepting '\e' as 'e'; (2) ensure i < cap-1 BEFORE writing to avoid off-by-one overflow (change line 339 to 'if(i >= cap-1) return 0'); (3) handle the case where the string ends without a closing quote by checking *p == '\"' on line 341 before incrementing.
- **[P2 c=0.75 fillable]** bad_path() only checks for '..' but not absolute path escapes — `userspace/apps/agentd/agentd.c:326-327`
  - The bad_path() check in agentd only rejects paths containing '..' substrings. However, it does not validate that paths are absolute or reject paths starting with invalid prefixes. If a tool incorrectly uses path_write_allowed() instead of bad_path(), or if bad_path() is the sole gate (as in tool_spawn), a relative path like 'etc/evil' might slip through. More critically, symlinks in /tmp can escap
  - *fix:* Strengthen bad_path() to reject relative paths (those not starting with '/') and explicitly whitelist only safe absolute prefixes. Example: reject if path[0]!='/' OR if path does not start with '/tmp','/home','/usr/src'. This makes bad_path() a complete duplicate of path_write_allowed() for consistency, or clearly separate them by scope (bad_path = quick prefix check; path_write_allowed = full pol
- **[P3 c=0.70]** tool_spawn and tool_exec use 'sbin/' prefix check, but tool paths are not verified to exist — `userspace/apps/tool_spawn/tool_spawn.c:81-86`
  - tool_spawn requires apps to start with 'sbin/', but it does not verify the file exists or that it is a valid executable. A model could request 'sbin/nonexistent', and the tool would output 'DENY spawn sbin/nonexistent' (actually, the gate passes because starts(app, 'sbin/') returns 1, then the spawn syscall fails and tool_spawn reports 'ERR spawn'). The issue is subtle: the gate is byname-only, no
  - *fix:* Add a stat() check before spawn to verify the target is a regular file (S_ISREG) or at minimum that the path exists and is not a symlink. This requires adding SYS_STAT and checking st_mode. Alternatively, tighten the policy by requiring sbin/ files to be signed/hashed at build time, but that's not localized.
- **[P3 c=0.65 fillable]** Missing explicit policy enforcement for 'rollback' tool in resolve_tool() compliance check — `userspace/apps/agentd/agentd.c:177-183`
  - is_confirm_tool() on line 177 hardcodes 'rollback' as a confirm tool (line 182), but rollback is not listed in the TOOLS array in nemotron_broker.js (line 45-59) or deepseek_broker.py (line 84-100). The brokers advertise to the model which tools are available, and the model may plan tool calls based on that list. If 'rollback' is not advertised but agentd accepts it, the model is unaware of it and
  - *fix:* Add 'rollback' to the TOOLS list in both nemotron_broker.js and deepseek_broker.py with the correct signature (line 95 in deepseek_broker.py already has it, but nemotron_broker.js does not). Ensure brokers and agentd maintain a single canonical TOOLS list.

## input-ps2 (6)
- **[P1 c=0.94]** evdev_open/evdev_close mutate client list without locking while IRQ handler iterates it — `kernel/drivers/input/evdev.c:135-137, 153-172`
  - evdev_open() (line 135-137) appends a new client to evdev->clients without any locking. evdev_close() (line 153-172) unlinks it without locking. Meanwhile, evdev_event() (line 68-82) iterates evdev->clients in a while loop called from IRQ handlers. If a client is opened or closed while an IRQ is processing events, the linked-list traversal in evdev_event() can dereference freed memory, skip client
  - *fix:* Protect all mutations and traversals of evdev->clients with the same spinlock used to protect the ring buffers (above). Acquire spin_lock_irqsave before traversing in evdev_event(), and hold it while inserting (evdev_open) and removing (evdev_close) clients.
- **[P1 c=0.92]** Unlocked ring buffer race: IRQ writes to evdev client->buffer while evdev_read() reads from it concurrently — `kernel/drivers/input/evdev.c:62-86, 184-216`
  - The evdev_event() function (called from IRQ handlers ps2_keyboard_irq_handler, ps2_mouse_irq_handler at lines 606, 743-758 of ps2.c) modifies client->buffer, client->head, and client->tail without any locking (lines 72-79). Concurrently, evdev_read() at line 184-216 reads from the same buffer without synchronization. On a multi-core system or with preemption, this causes a classic TOCTOU race: the
  - *fix:* Protect the evdev client ring buffer operations with a spinlock (spin_lock_irqsave / spin_unlock_irqrestore) around all read/write accesses in evdev_event(), evdev_read(), and evdev_open/evdev_close client list mutations. Store the spinlock in evdev_device_t or evdev_client_t.
- **[P2 c=0.98]** evdev_read() always returns 0 (non-blocking) when buffer is empty, even for blocking clients — `kernel/drivers/input/evdev.c:198-203`
  - The evdev_read() function checks if a client is non-blocking (line 199), but if the buffer is empty and the client IS blocking, it still returns 0 (line 203) instead of waiting for input. The TODO comment on line 202 acknowledges this is not implemented. Blocking reads on /dev/input/eventN will return 0 immediately instead of sleeping, making it impossible for userspace to wait for input events—th
  - *fix:* Implement a wait queue or condition variable for blocking clients. Store a wait_queue_t* in evdev_client_t or evdev_device_t, and call wait_queue_wait() in evdev_read() when the buffer is empty and nonblock=false. Wake the queue in evdev_event() when new events arrive.
- **[P2 c=0.95 fillable]** Mouse coordinate clamping hardcoded to 800x600, actual display is 1024x768 — `kernel/drivers/ps2.c:733-737`
  - The PS/2 mouse driver hardcodes screen bounds to 800x600 (lines 734-737: clamping to 799x599), but the actual compositor framebuffer is 1024x768 (confirmed in userspace/compositor/fb.c). This causes the mouse cursor to be clamped to the wrong screen size, preventing users from reaching the right and bottom 224x169 pixels of the display.
  - *fix:* Replace hardcoded bounds with constants or dynamic detection. Either define MOUSE_MAX_X=1023 and MOUSE_MAX_Y=767 at the top of ps2.c, or add a function to query the actual display resolution from the compositor/framebuffer driver.
- **[P3 c=0.85 fillable]** Default mouse initial position is hardcoded to 400,300; does not match display resolution or center — `kernel/drivers/ps2.c:118-119`
  - The mouse X and Y coordinates are initialized to 400 and 300 (lines 118-119), which approximates a center position for a 800x600 display. However, the actual display is 1024x768 (userspace/compositor/fb.c:30-31), so the true center should be 512,384. This causes the mouse to start off-center and may confuse users or desktop initialization routines that assume the cursor starts in a sane position.
  - *fix:* Change the initialization to mouse_x = 512; mouse_y = 384; (or better: compute from the actual display width/height once that is made accessible to the driver).
- **[P3 c=0.80 fillable]** No overflow count in evdev client buffers; events silently drop without notification — `kernel/drivers/input/evdev.c:75-80`
  - When an evdev client's buffer is full (next_tail == head), the driver silently discards the oldest event (line 77) and writes the new event in its place. There is no counter or flag to notify the reader that data was lost. The global input subsystem tracks overflow_count (input.c:28), but evdev never increments a per-client counter. Applications reading from /dev/input/eventN have no way to know i
  - *fix:* Add an overflow_count field to evdev_client_t. Increment it when dropping an event in the buffer-full case (line 77). Provide an ioctl or sysfs interface to read the count, or include it in an extended event structure so userspace can detect dropped frames.

## sched-coop (6)
- **[P1 c=0.90 fillable]** RACE-001 unlock/relock window in scheduler_yield_requeue() enables double-enqueue on SMP — `kernel/core/sched/scheduler.c:2430-2459`
  - scheduler_yield_requeue() has an unsafe unlock-relock pattern at lines 2436-2440. Between unlock(2436) and relock(2440), another CPU's scheduler_remove_process() can clear on_queue, but the requeue code does NOT re-check on_queue after relocking. This allows a race where the same process gets double-enqueued: (1) CPU0 unlocks after checking on_queue==0, (2) CPU1 calls remove_process() and clears o
  - *fix:* Consolidate into a single critical section by moving process_ref(proc) into the locked region, OR re-check on_queue after relocking at line 2440 before proceeding with enqueue.
- **[P1 c=0.85 fillable]** scheduler_yield_requeue() missing re-check for on_queue after unlock-relock — `kernel/core/sched/scheduler.c:2434-2440`
  - Between lines 2436 (unlock) and 2440 (relock), no guardrail ensures on_queue is still 0. A concurrent scheduler_remove_process() (SMP) could set on_queue=0, removing the process. When line 2440 relocks, lines 2441-2457 proceed to enqueue unconditionally. The idempotency guard at line 2435 is bypassed due to the gap. Code comment acknowledges gap is 'pre-existing' but this does not make it safe on 
  - *fix:* Add 'if (proc->on_queue) { spin_unlock(...); return; }' immediately after line 2440's lock acquisition, before modifying the process state.
- **[P2 c=0.65 fillable]** Stale on_queue check in KILL-FIX-001 drain path allows RUNNING process to remain queued — `kernel/core/sched/scheduler.c:2600-2612`
  - In scheduler_pick_next(), if a TERMINATED process is dequeued (lines 2600-2612), the code correctly drains it, decrements ready_count, and re-picks. However, there is no parallel guard for a RUNNING process that appears in the queue (should never happen but is not explicitly defended). The KILL-FIX-001 comment acknowledges TERMINATED can transiently appear (sys_kill sets TERMINATED after enqueue),
  - *fix:* Add an explicit check after KILL-FIX-001: if (next->state == PROCESS_RUNNING) { panic or drain similar to TERMINATED }. Or strengthen the loop comment to explain why RUNNING cannot occur (and add an assertion).
- **[P2 c=0.45 fillable]** Missing NULL check for dead process context before resume in cooperative_switch_to() — `kernel/core/sched/scheduler.c:2703-2726`
  - cooperative_switch_to() is called with a TERMINATED process as 'from' (line 2829). The function does not verify that 'from' is RUNNING before attempting to save its context. In schedule(), when current is TERMINATED, dead->resume_mode is set to RESUME_CRETURN and context_switch() is called. If context_switch_asm reads from dead->context fields that have not been properly initialized (e.g., if the 
  - *fix:* Add an assertion or explicit check: if (from && from->state == PROCESS_TERMINATED) { panic('should not save dead context') } at the entry to context_switch_asm, to catch misuse early.
- **[P2 c=0.30]** Missing idempotency re-check in scheduler_add_process() after the single critical section — `kernel/core/sched/scheduler.c:2304-2398`
  - scheduler_add_process() wraps the on_queue test, ref, and enqueue in a single critical section (RACE-001 fix) at lines 2335-2390. However, if called twice in rapid succession on the same process (e.g., from two different CPU cores issuing scheduler_add_process_to_cpu and then scheduler_add_process on the same task), the FIRST call's unlock-relock inside the spin-lock region is missing. While the f
  - *fix:* No fix needed; the critical section is correctly maintained throughout. Disregard this finding.
- **[P3 c=0.55 fillable]** Runqueue bitmap can become desynchronized with queue contents if a corrupt process is partially unlinked — `kernel/core/sched/scheduler.c:273-285`
  - bitmap_clear() is called from runqueue_dequeue() only when a queue becomes empty (line 351). However, if a process is forcibly removed (e.g., via a kill signal that sets PROCESS_TERMINATED while enqueued), and the removal path fails to dequeue it (or a stray process remains after a partial dequeue), the bitmap bit will remain set even though the queue is empty. The SCHED_DEBUG validators (sched_db
  - *fix:* In scheduler_pick_next(), after runqueue_pick_next() returns NULL and before returning idle, explicitly call bitmap_clear() for that priority to guarantee bit synchronization. Or add a bounded spin in bitmap_ffs() with a panic if we exceed a maximum iteration count.

## mem-pmm (6)
- **[P1 c=0.90 fillable]** Direct-map shadow vulnerability in pmm_free_page() SLAB_MAGIC check — `kernel/core/mem/pmm.c:533`
  - pmm_free_page() at line 533 dereferences the page parameter (a physical address) to check for SLAB_MAGIC without using PHYS_TO_DIRECT(). The code comments in mem.h:14-22 explicitly document that raw physical-address dereferencing is unsafe under process context (the low identity map can be shadowed by user ELF/data). If a context switch occurs with an active process that maps data at low VA, this 
  - *fix:* Change line 533 from `if (*(volatile uint64_t*)page_addr == 0x51AB0BACE51AB0BULL)` to `if (*(volatile uint64_t*)PHYS_TO_DIRECT(page_addr) == 0x51AB0BACE51AB0BULL)` to safely dereference through the shared DIRECT_MAP (PML4[256]).
- **[P1 c=0.90 fillable]** Direct-map shadow vulnerability in pmm_free_pages() SLAB_MAGIC check — `kernel/core/mem/pmm.c:898`
  - Same issue as above: pmm_free_pages() at line 898 dereferences page_addr (a physical address derived from addr_of_pfn()) without PHYS_TO_DIRECT() to check SLAB_MAGIC. This can read a shadowed process page under process context, corrupting the guard and potentially freeing live slab pages.
  - *fix:* Change line 898 from `if (*(volatile uint64_t*)page_addr == 0x51AB0BACE51AB0BULL)` to `if (*(volatile uint64_t*)PHYS_TO_DIRECT(page_addr) == 0x51AB0BACE51AB0BULL)` for safe access through the DIRECT_MAP.
- **[P2 c=0.95]** Slab cache pool never reclaimed after slab_cache_destroy() — `kernel/core/mem/slab.c:234-240, 460-468`
  - The slab allocator uses a bump allocator for the cache pool (MAX_SLAB_CACHES=64 fixed slots). When slab_cache_destroy() is called, the slot is NOT reclaimed (see line 460 comment). This means cache_pool_used is monotonically increasing and never decreases. After creating and destroying caches repeatedly, eventually cache_pool_used >= 64 and slab_cache_create() will fail permanently with 'cache poo
  - *fix:* Implement either: (1) slot reclamation on destroy (maintain a free-list), (2) use a bitmap of used slots to allow reuse, or (3) increase MAX_SLAB_CACHES. Simplest fix: track destroyed slots and reuse them in slab_cache_create().
- **[P2 c=0.80 fillable]** Direct-map shadow vulnerability in pmm_alloc_page() debug probes — `kernel/core/mem/pmm.c:391, 443`
  - pmm_alloc_page() at lines 391 and 443 dereferences returned page pointers to check SLAB_MAGIC in debug printf statements. While these are just informational probes, if the condition is evaluated it accesses raw physical addresses without PHYS_TO_DIRECT(). Under process context, this could trigger a #PF or read corrupted data. The debug output suggests these were added to catch a 'slab churn' bug, 
  - *fix:* Wrap the SLAB_MAGIC checks with PHYS_TO_DIRECT(): change `if (page && *(volatile uint64_t*)page == ...)` to `if (page && *(volatile uint64_t*)PHYS_TO_DIRECT(page) == ...)` on lines 391 and 443.
- **[P3 c=0.70 fillable]** Race condition in pmm_report_cache_stats() reads unprot ected cache statistics — `kernel/core/mem/pmm.c:1009-1021`
  - pmm_report_cache_stats() reads per-CPU cache statistics (alloc_fast, alloc_slow, count) without holding the per-CPU cache locks. Other CPUs can update these fields concurrently, leading to inconsistent reads (e.g. reading alloc_fast at time T1 and alloc_slow at time T2, producing nonsensical ratios). For a statistics function this is tolerable, but the 'BUG-015 fix' comment suggests awareness of d
  - *fix:* Either: (1) acquire each cache->lock while reading, (2) use __atomic_load() for atomic snapshots, or (3) document that stats are approximate. For production use, snapshot each cache's stats atomically.
- **[P3 c=0.60 fillable]** Missing bounds check for integer overflow in pmm_free_pages() initrd validation — `kernel/core/mem/pmm.c:867`
  - The calculation at line 867 `uint64_t e = s + (uint64_t)count * MIN_PAGE_SIZE` can overflow if base is near the top of address space and count is large. The subsequent overlap check `if (s < pmm_initrd_end && e > pmm_initrd_start)` would then use a wrapped address. In practice, physical addresses are bounded to 0..16GB so overflow is unlikely, but the pattern mirrors vmm.c:321 which explicitly han
  - *fix:* Check for overflow before computing end address, similar to vmm.c: change to check if (count > UINT64_MAX / MIN_PAGE_SIZE) before the multiply, or clamp count to prevent overflow.

## net-sock (6)
- **[P1 c=0.90 fillable]** Ephemeral port collision: no collision detection during sock_alloc_port() — `kernel/net/socket.c:57-61`
  - sock_alloc_port() allocates ports sequentially without checking for collisions. When 32 sockets exist at port 65535 and wrap to 49152, new sockets will collide with existing bound ports. sock_bind() has collision detection (line 473-476), but sock_socket() allocates without checking (line 454), and sock_connect() does not validate either. A socket created after wrapping can silently overwrite an e
  - *fix:* After incrementing g_next_port in sock_alloc_port(), verify the port is not already in use by scanning the socket table (similar to sock_bind's loop at line 473-477). Return 0 or retry if collision detected.
- **[P1 c=0.85 fillable]** sock_find_tcp first pass matches TCP_CLOSED state: incorrect for passive children — `kernel/net/socket.c:297-309`
  - sock_find_tcp's first pass skips sockets only when state==TCP_LISTEN OR state==TCP_CLOSED (line 302). However, a just-promoted TCP_ESTABLISHED child from synq_on_ack (tcp.c line 402) will match in the first pass if an identical incoming segment arrives during the accept queue window. This can cause the listener to receive the ACK instead of the child socket, breaking accept() sequencing. The secon
  - *fix:* Line 302 condition should be: if (s->state == TCP_LISTEN) continue; (remove TCP_CLOSED). TCP_CLOSED sockets should never match; they have no active connection. Verify the logic: first pass finds ESTABLISHED/SYN_RCVD/etc., second pass finds LISTEN only.
- **[P2 c=0.75 fillable]** sock_accept() leaks child socket if descriptor lookup fails — `kernel/net/socket.c:513-537`
  - sock_accept() dequeues a child from the accept_queue (lines 522-524) and clears its links (lines 524-525), but if the descriptor lookup loop (lines 529-533) fails to find the child's index, it returns SOCK_EBADF without re-adding the child to the queue or marking used=false. The child remains used=true but orphaned from the listener. On the next accept(), the queue iteration would find it again, b
  - *fix:* Store the child pointer before dequeuing, then restore it to the queue if the lookup fails (nested for with early break + error handling), or assert that the lookup always succeeds. Alternatively, scan for the child's index BEFORE dequeuing.
- **[P2 c=0.65]** UDP queue full: silently drops datagrams without signaling sender — `kernel/net/udp.c:82-83`
  - When a UDP socket's datagram queue is full (dq_count >= UDP_QUEUE_DEPTH at line 83), the incoming datagram is silently dropped. No ICMP unreachable is sent, and no flow control signal is generated. The sender is unaware and may retry (e.g., DNS query), but repeated drops waste bandwidth. This is documented as a limitation in socket.c (commented as 'best-effort'), but for high-traffic scenarios a b
  - *fix:* Optional enhancement: send ICMP Destination Unreachable (code 1, source quench) when dropping due to queue full, following RFC 1122. This signals the sender to back off. Implementation: call net_icmp_send_unreachable() (if available, else add it).
- **[P2 c=0.60 fillable]** sock_poll() drain limited to 64 frames without abort-on-runt validation — `kernel/net/socket.c:370-380`
  - sock_poll() drains up to 64 frames per call (line 370). The runt frame check (line 374: if ((uint16_t)n < ETH_HLEN) continue;) skips frames smaller than 14 bytes but does NOT validate that ipv4_demux receives at least 20 bytes (sizeof(ipv4_hdr_t)). A malformed frame with 14 < n < 20 passes the ETH_HLEN check but ipv4_demux will silently return at line 329. No issue here, but the naming/comment at 
  - *fix:* Clarify the check at line 374 with a comment: 'IP packet payload must have at least an IPv4 header'. Consider making the loop count adaptive (break if n==0 two times in a row) or documented as a deliberate rate-limit. For now, a comment fix is sufficient.
- **[P3 c=0.50]** TCP accept queue promotes child with listener->local_port, but no validation of backlog backpressure during accept — `kernel/net/tcp.c:364-379`
  - synq_on_ack() counts pending children (line 366-368) and enforces backlog (line 369-371). However, there is no concurrent-modification guard: between the count and the socket allocation, sock_close() on a child socket could decrement pending, allowing a second synq_on_ack() to exceed the backlog. In a single-threaded kernel this is not exploitable, but the invariant is not formally enforced. The b
  - *fix:* Atomically check backlog and allocate in a single critical section, or add a backlog_used counter that is incremented when a child is added and decremented in sock_close() for children (line 617-627). This is low priority because the kernel is single-threaded.

## smp (6)
- **[P1 c=0.85]** cpu_id() returns 0 unconditionally in default build, masking CPU identity — `kernel/stubs.c:33`
  - In non-SMP_SCHED builds (the default), cpu_id() is stubbed to always return 0, even if SMP_FOUNDATION is enabled and the AP is running. This means preempt_disable(), ipi_consume_need_resched(), and other per-CPU operations all operate on CPU 0's state even when called from CPU 1. The IPI code works around this by using smp_cpu1_apic_id() instead, but any code path that calls cpu_id() directly will
  - *fix:* Either (1) enable SMP_SCHED by default when SMP_FOUNDATION is set, or (2) provide a fall-back cpu_id() implementation in ap_boot.c that reads lapic_get_id() and compares against g_ap1_apic_id even without SMP_SCHED. The latter is safer: `#ifdef SMP_FOUNDATION` in stubs.c to weak-export the stub so ap_boot.c can override it.
- **[P2 c=0.60 fillable]** AP preempt_count not explicitly initialized — `kernel/arch/x86_64/ap_boot.c:434-554`
  - The AP's ap_main() does not initialize percpu_data[1].preempt_count or cpus[1].preempt_count. In the SMP_SCHED_DISPATCH path, when ap_schedule_from_irq() is called, the scheduler may check preempt_count. While this is in .bss so it's zero at boot, once ap_main runs and enables interrupts, any preempt_disable/enable calls will operate on an uninitialized field. If the AP runs user code that calls p
  - *fix:* After loading CPU1's TSS in gdt_ap_load_tss() and before enabling interrupts, explicitly initialize the AP's preempt state: `percpu_data[1].preempt_count = 0;` or add a call to `percpu_init(1);` in the AP's ap_main() before the sti.
- **[P2 c=0.50 fillable]** Scheduler lock retained as global lock for shutdown, still protects enqueue on non-SMP_SCHED builds — `kernel/core/sched/scheduler.c:204, 1721`
  - The scheduler_lock is documented as retained ONLY for scheduler_shutdown (drain serializer). However, the scheduler.c is compiled in two versions: (1) non-CONFIG_SMP single-CPU version (the default, lines 44-99), which uses a single global runqueue and scheduler_lock, and (2) CONFIG_SMP version with per-CPU runqueues (lines 322+ in scheduler_smp.c). The non-CONFIG_SMP version still exists and uses
  - *fix:* Add a compile-time assertion in scheduler.c: `#if defined(CONFIG_SMP) && defined(SMP_SCHED) #error "scheduler.c and scheduler_smp.c both compiled; conflict on scheduler_init"`. Or use weak symbols so only one definition wins. Document the mutual exclusion in scripts/quick_build.sh.
- **[P2 c=0.40 fillable]** IPI call queue elements not zero-initialized on allocation — `kernel/arch/x86_64/ipi.c:134-137`
  - The IPI call queue array is static .bss, so elements ARE zero-initialized at program start. However, if the queue is reused after a timeout or error, stale function pointers or data pointers from previous queue entries could remain. The dequeue_call() copies entries by value, so a stale entry with a non-NULL func pointer could be executed on the next cycle if head/tail indices are mismanaged. The 
  - *fix:* Add explicit zeroing of call_queue during ipi_init(): `for (uint32_t cpu = 0; cpu < IPI_MAX_CPUS; cpu++) { for (uint32_t i = 0; i < IPI_CALL_QUEUE_SIZE; i++) { call_queue[cpu][i].func = NULL; call_queue[cpu][i].data = NULL; call_queue[cpu][i].done_ptr = NULL; } }`
- **[P3 c=0.40 fillable]** IPI call queue size (16) fixed, no dynamic resizing for many-CPU systems — `kernel/arch/x86_64/ipi.c:127`
  - The IPI call queue is fixed at 16 entries per CPU. If a CPU receives more than 16 pending IPI function calls before it drains them, enqueue_call() returns -1 (queue full). The callers (ipi_call_function, ipi_call_function_many) then print a warning and return -1, but do NOT retry or queue to a secondary queue. This is fine for the 2-CPU SMP_FOUNDATION system, but if extended to 8+ CPUs and a CPU r
  - *fix:* Add bounds checking and clear documentation: either increase the queue size (carefully, as .bss bloat is mentioned in the comments), or add a warning to ipi.h that callers must handle -EAGAIN from ipi_call_function and retry. The overflow condition should be logged loudly (not just printed to stdout). Consider: `#define IPI_CALL_QUEUE_SIZE 32` if .bss budget allows, or add a compile-time assertion
- **[P3 c=0.35 fillable]** No per-CPU kernel RSP initialization on AP for non-SMP_SCHED syscall path — `kernel/arch/x86_64/ap_boot.c:446-453`
  - In the non-SMP_SCHED (coprocessor) build, ap_main() calls syscall_msr_init_ap() to set CPU1's SYSCALL MSRs, but kernel_rsp_save_arr[1] is only written if CPU1 calls a syscall. If CPU1 attempts a syscall before its kernel_rsp_save_arr[1] is populated by a context switch (which normally does tss_set_kernel_stack()), the AP would load RSP=0 (uninitialized .bss) from the MSR and crash on entry. In pra
  - *fix:* After syscall_msr_init_ap(), explicitly set the AP's kernel RSP in ap_main(): `extern uint64_t kernel_rsp_save_arr[2]; kernel_rsp_save_arr[1] = (uint64_t)&ap_stack[AP_STACK_SIZE];` to match the AP's boot stack. Or in SMP_SCHED path, call tss_set_kernel_stack() before sti to ensure both TSS.RSP0 and kernel_rsp_save_arr[1] are synchronized.

## nic-e1000 (6)
- **[P1 c=0.85 fillable]** RX End-of-Packet (EOP) status bit not validated — `kernel/drivers/net/e1000.c:1228-1230`
  - e1000_rx_poll() checks only DD (Descriptor Done) bit but never validates RXD_STAT_EOP (bit 1), which indicates packet boundary. On hardware that splits large frames or handles fragmented packets across multiple descriptors, consuming a frame without EOP set will silently drop subsequent descriptor(s) containing the rest of the packet. QEMU's e1000 emulation only sends single-descriptor frames, so 
  - *fix:* Add a check: if (!(d->status & RXD_STAT_EOP)) { kprintf(warning); } before processing, or refuse to advance rx_cur until EOP is seen. For now, a simple workaround: check `if (!(d->status & RXD_STAT_EOP)) continue;` to wait for the final descriptor.
- **[P2 c=0.95]** IRQ permanently masked with no interrupt service routine — `kernel/drivers/net/e1000.c:1026, 1027, 1071`
  - Device interrupts are permanently masked (E1000_IMC = 0xFFFFFFFF) and no ISR is registered via irq_register_handler(). Driver is hard-coded to poll-mode only. This is documented as intentional design in the header (e1000.h line 39), but means: (1) received frames are NOT delivered via interrupts—caller must pump e1000_rx_poll() in a loop; (2) if polling stops (scheduler preemption, vCPU pause), fr
  - *fix:* This is a design decision, not a bug. If interrupt-driven RX is needed, register a real ISR that calls e1000_rx_poll() and delivers to higher layers. For now, ensure kernel's main loop and all paths that might block call net_recv() frequently enough to not drop frames (a scheduler yield or tick handler).
- **[P2 c=0.75 fillable]** Batch transmit timeout silently succeeds with unverified hardware completion — `kernel/drivers/net/e1000.c:1159-1164`
  - e1000_transmit_batch() spins for 100k iterations waiting for the LAST descriptor's DD (Done) bit. If the spin times out (line 1163), the function still returns queued count as if transmission succeeded. The comment says 'almost certainly queued', but a hardware stall, bus failure, or reset during transmission could leave frames in the ring without driver knowledge, causing silent data loss on retr
  - *fix:* Log a warning on timeout: `if (spin >= 100000) kprintf('[E1000] TX batch timeout at desc %u queued=%u', last_i, queued);` Return -1 on timeout to signal failure, or track in a separate return field. Callers should retry or fail gracefully instead of assuming success.
- **[P3 c=0.90 fillable]** Header documentation contradicts actual ring configuration — `kernel/include/e1000.h:101-102`
  - e1000.h declares 'RX ring: NUM_RX_DESC=32 descriptors + 2048-byte buffers' and 'TX ring: NUM_TX_DESC=8 descriptors' as part of the init() contract. Actual code (e1000.c line 249-250) defines both as 64. Integrators relying on the documented size to calculate buffer pool allocation or validate workloads will underestimate ring capacity, leading to capacity planning errors or subtle bugs in stress t
  - *fix:* Update kernel/include/e1000.h lines 101-102 to say NUM_RX_DESC=64 and NUM_TX_DESC=64, or update e1000.c defines to match the documented 32/8 if that was the intended design.
- **[P3 c=0.70 fillable]** RX descriptor ring is never drained in batch; only single frame per poll — `kernel/drivers/net/e1000.c:1216-1260`
  - e1000_rx_poll() processes exactly one frame per call, advancing rx_cur by 1. Under bursty inbound traffic (e.g. ARP + ICMP floods), if the polling frequency cannot keep up, RX ring can overflow before descriptors are recycled. Contrast with TX path (e1000_transmit_batch) which processes multiple frames in one call. RX has no batching equivalent, forcing inefficient 64 poll calls to drain a full ri
  - *fix:* Add an e1000_rx_drain() or extend e1000_rx_poll() to accept a max_count parameter, draining up to N frames in a single call. Callers can batch-process via e.g. `while (e1000_rx_poll_batch(bufs, MAX, &count)) ...`. Requires wiring into net_recv().
- **[P3 c=0.65 fillable]** PCH 82577LM hardware bring-up deferred but no timeout or status check — `kernel/drivers/net/e1000.c:941-968`
  - When PCH device is detected, e1000_init() returns -1 (line 949, 968) and sets bringup_deferred=true, expecting e1000_pch_deferred_bringup() to be called later via SYS_NET_CONFIG NIC_BRINGUP trigger. However, if that trigger is never invoked (user forgets to run nicup tool, or trigger infrastructure is missing), e1000.present remains false and all network calls silently fail with no warning after i
  - *fix:* Add e1000_pch_bringup_pending() API that returns true if bringup_deferred is set. Ensure kernel main loop or a diagnostic syscall checks this state and alerts integrator. Alternatively, raise a boot warning: kprintf('[E1000] PCH detected but bring-up deferred—run nicup to complete').

## net-tcp (6)
- **[P1 c=0.85 fillable]** cwnd accounting resets incorrectly after partial ACK, losing in-flight count — `kernel/net/tcp.c:716`
  - After sending a segment and polling for ACKs (line 714), if rt_pending clears (indicating the retransmit machinery was cleared by an incoming ACK), the code resets in_flight to 0. This is incorrect because in_flight tracks segments sent but not yet ACKed, and a single ACK clearing rt_pending doesn't mean all in-flight segments were ACKed - only the last one tracked by rt_data was acknowledged. Wit
  - *fix:* Line 716 should not blindly reset in_flight. Instead, in_flight should only be decremented when snd_una actually advances (detected in the wait loop at lines 620-626). Remove line 716 and ensure the only place in_flight is set to 0 is after explicit analysis of ACK advancement.
- **[P1 c=0.75 fillable]** Overlapping OOO segments not deduplicated, can cause data corruption on merge — `kernel/net/tcp.c:962`
  - When storing OOO segments, the code only checks for exact sequence match (slot->seq == seq) to deduplicate. If two segments partially overlap but have different starting sequence numbers (e.g., seq 100-200 already stored, then receive seq 150-250), both are stored in separate slots. When tcp_ooo_drain merges them, the trim logic may create gaps or corruption: if rcv_nxt=0 we merge [100-200], then 
  - *fix:* When storing an OOO segment, check not just for exact seq match but also for overlaps: for each existing slot, verify that the new segment [seq, seq+len) does not overlap with [slot->seq, slot->seq+slot->len). If it overlaps, either reject the new segment (peer retransmits cleanly) or merge/combine them. At minimum, check that SEQ32_GT(seq + payload_len, slot->seq) to detect overlaps.
- **[P2 c=0.90]** Tail-only retransmit design cannot recover lost earlier segments in pipeline — `kernel/net/tcp.c:708`
  - The retransmit machinery (s->rt_data, s->rt_seq, s->rt_len) stores only the LAST segment sent (line 708 comment: 'only the tail of the pipeline is retransmittable'). With TCP_CWND_INIT=4 allowing up to 4 segments in flight, if segment 2 of 4 is lost but segment 3-4 arrive, the lost segment cannot be retransmitted because s->rt_data was overwritten when segments 3 and 4 were sent. Recovery relies o
  - *fix:* Design limitation: would require either (a) larger rt_data buffer + ring to track all in-flight segments, (b) selective retransmit (SACK), or (c) reduce TCP_CWND_INIT to 1 (stop-and-wait). Current design is explicitly documented as a limitation (line 706 comment). No easy localized fix without architectural change.
- **[P2 c=0.60 fillable]** TCP_SYN_RCVD state has no timeout, can leak dead sockets — `kernel/net/tcp.c:1042`
  - The tcp_tick() function implements RTO timeout + retransmit for SYN_SENT, ESTABLISHED, and FIN_WAIT (line 1042-1046), but NOT for TCP_SYN_RCVD. If a server-side socket reaches SYN_RCVD state and the final ACK of the 3-way handshake is lost, the socket will remain in SYN_RCVD indefinitely with no timeout, leaking the socket slot until the process/listener dies. Note: in the current passive-open-via
  - *fix:* Add TCP_SYN_RCVD to the timeout check at line 1042: change condition to include s->state == TCP_SYN_RCVD, or verify that SYN_RCVD is truly unreachable and remove the dead case handler at lines 856-872.
- **[P3 c=0.95 fillable]** Documentation stale: OOO buffer described as 'single slot' but implemented as 4 slots — `kernel/net/tcp.c:49`
  - The header comment at lines 49-50 states: 'OOO buffer is a single slot per socket (handles the common case of one out-of-order segment...)'. However, TCP_OOO_DEPTH is defined as 4 (line 107), and the implementation clearly supports 4 parallel OOO slots per socket (lines 960-965 iterate through all 4 slots, evicting by 'FARTHEST from rcv_nxt'). This discrepancy between documentation and implementat
  - *fix:* Update lines 49-52 comment to reflect the actual implementation: 'OOO buffer is TCP_OOO_DEPTH=4 slots per socket, allocated heap-backed. If a larger array is needed, update TCP_OOO_DEPTH and the allocation in tcp_buffers_init().' Or add a note referencing NET-P1-B at line 95 that explains the 4-slot design.
- **[P3 c=0.70 fillable]** Out-of-order FIN not handled: if FIN arrives before earlier data, it's ignored — `kernel/net/tcp.c:1011`
  - The FIN handling code (lines 997-1013) only processes FIN if it arrives in-order (seq == rcv_nxt or seq + payload_len == rcv_nxt). If a FIN arrives out-of-order (seq > rcv_nxt), the comment at line 1011-1012 acknowledges this limitation: 'If FIN arrived OOO just ACK current position; we'll re-process it when the gap fills (limitation: no FIN in OOO slot)'. However, when the gap fills and rcv_nxt a
  - *fix:* To properly handle OOO FIN: (1) extend tcp_ooo_slot_t to include a 'has_fin' flag, (2) in tcp_input OOO handler, save the FIN flag if present, (3) in tcp_ooo_drain, after pushing data, check if the slot's has_fin flag is set and transition state accordingly. Or accept as documented limitation and rely on retransmit/timeout.

## fb (6)
- **[P1 c=0.85]** MTRR-WC completely defeated by firmware UC marking—silent degradation — `kernel/drivers/framebuffer.c:220-221`
  - Lines 220-221 acknowledge that MTRR overlap rules decree UC wins: if firmware marks the FB region with a UC MTRR or UC fixed-range MTRR, the kernel's WC MTRR has no effect and the framebuffer remains uncached. The code only logs a reminder but continues silently, leading to ~10 fps performance on real hardware (mentioned in lines 15-20) with no way to recover. This is a vision-vs-reality gap: the 
  - *fix:* Probe or query firmware MTRR settings before attempting to set WC MTRR. If firmware UC is detected, either: (1) log a HARD WARNING, (2) attempt PAT WC as fallback, (3) attempt to disable/modify firmware MTRR if safe, or (4) document that WC will fail on this platform and accept UC performance. Add a runtime check to verify MTRR took effect by reading back the register and confirming the WC MTRR is
- **[P1 c=0.80 fillable]** Missing SFENCE after WC MTRR writes and before framebuffer access — `kernel/drivers/framebuffer.c:205-210`
  - Intel SDM requires an SFENCE instruction after setting up Write-Combining memory attributes before any WC memory access. The code performs WBINVD but lacks SFENCE between re-enabling MTRRs (line 203) and restoring CR0 (line 208). Without SFENCE, WC coalescing buffers on some CPUs may not be enabled when framebuffer_clear() is called immediately after fb_enable_write_combining() in kernel.c, causin
  - *fix:* Add `__asm__ volatile("sfence" ::: "memory");` after line 207 (after the final CR0 restore write_cr3 call) and before sti(). Alternatively, add it right after the DEF_TYPE MTRR re-enable at line 203 to ensure WC buffering is active before any memory access.
- **[P2 c=0.75]** No PAT (Page Attribute Table) implementation as fallback to MTRR — `kernel/drivers/framebuffer.c:115-222`
  - The framebuffer WC setup relies entirely on MTRRs (lines 115-222). Modern x86_64 hardware (Nehalem+) supports PAT as a superior alternative to MTRRs for per-page memory type control, which is more flexible and doesn't require firmware coordination. The code has no PAT initialization or fallback path. On systems where all variable MTRRs are occupied by firmware or where firmware has UC entries cove
  - *fix:* Implement a fallback PAT-based WC path: (1) Initialize IA32_PAT MSR if not already done by firmware, (2) set up page-table entries for the FB region with PAT index pointing to WC type, (3) attempt PAT WC before MTRR WC, or (4) document why PAT was deferred and is not a supported path.
- **[P2 c=0.70 fillable]** Application Processors (APs) boot with default WB memory type, never get WC setup — `kernel/arch/x86_64/ap_boot.c:434-554`
  - The AP main function (ap_main, lines 434-554) performs CPU-local setup (TSS, SYSCALL MSRs, SSE) but never calls fb_enable_write_combining() or any equivalent PAT/MTRR setup. The AP inherits the BSP's page tables (same CR3) but each CPU has its own MTRR and PAT MSR state. The AP's default MTRR_DEF_TYPE remains at WB or whatever firmware set, so any framebuffer access by the AP (if it ever touches t
  - *fix:* Call fb_enable_write_combining(fb_phys_base, fb_size) in ap_main() after the CPU-local TSS/SYSCALL setup (after line 453). Store the FB physical base and size as global kernel variables set during BSP initialization in kernel.c so the AP can access them. Alternatively, pass them as part of the AP parameter block (ap_trampoline.asm ap_param_block) if AP boot already has spare capacity.
- **[P2 c=0.65 fillable]** No validation that MTRR WC actually took effect—register not read back — `kernel/drivers/framebuffer.c:213-222`
  - After writing PHYSBASE/PHYSMASK and re-enabling MTRRs (lines 199-203), the code only logs what it *intended* to write (lines 213-222) but never reads back the MSRs to confirm they were actually accepted by the CPU. If the wrmsr calls silently failed (e.g., due to microcode issues or invalid values), or if firmware immediately overwrote them, the code would have no way to know. The log message note
  - *fix:* After line 203 (after re-enabling MTRRs), add a read-back of the PHYSBASE/PHYSMASK registers to confirm they match what was written. If they don't match or the valid bit is clear, log an error and set a flag so callers know WC failed. Alternatively, add a runtime check in framebuffer_clear() or fill_row_u64() to detect UC behavior (e.g., measure latency or compare against a WC baseline) and warn i
- **[P3 c=0.60 fillable]** PHYSMASK_BITS calculation truncates on CPUs reporting > 52 bits of physical address — `kernel/drivers/framebuffer.c:86-94`
  - The fb_wc_physmask_bits() function clamps reported physical address width to 52 bits (line 87). If a CPU reports > 52 bits (valid up to ~56 bits on some Xeons), the code silently clips it to 52, which could generate an under-wide MTRR mask that does not cover the full physical range intended. While rare on consumer/T410 hardware, this is a potential data corruption vector on high-capacity systems.
  - *fix:* Update the sanity bound to match documented x86_64 physical address width limits (up to 56 bits on current CPUs, future-proof to 64). Check against a macro constant (e.g., MAX_PHYS_BITS) rather than a hard-coded 52. Add a kprintf warning if the bound is exceeded, so operators know the MTRR may be undersized.

## comp-input (6)
- **[P2 c=0.92 fillable]** Right dock icon hover state not tracked for damage — `userspace/compositor/compositor_m8.c:3127, 3161, 5349-5353`
  - g_rdock_hovered is set fresh every frame in rdock_render_dock() but never compared to its previous value. Unlike desktop icon hover (g_desk_hover, lines 5349-5353) which tracks state changes and forces full recomposites via g_full_damage_cooldown, the right dock hover changes are completely undetected. When the cursor enters/exits the hover zone of a right dock icon, the visual highlight change ma
  - *fix:* Add a static int g_rdock_hovered_prev variable, track its value, and when g_rdock_hovered != g_rdock_hovered_prev, call mark_dirty() and set g_full_damage_cooldown = FULL_DAMAGE_COOLDOWN_FRAMES before the render loop returns, matching lines 5349-5352 for desktop hover.
- **[P2 c=0.78 fillable]** Window titlebar hover detection incomplete boundaries — `userspace/compositor/compositor_m8.c:4922-4924`
  - The hover check for window titlebar recomposite triggers (lines 4922-4924) only tests if cursor is over win->x/win->y to win->x + win->w / win->y + TITLEBAR_H. This misses the BORDER_W borders that are drawn outside the client rect; the actual rendered titlebar footprint extends BORDER_W pixels left/right and above. A cursor move over a window border may fail to trigger recomposite even though it'
  - *fix:* Expand the titlebar hit-test rect by BORDER_W on all sides: check if g_cursor_x is in [win->x - BORDER_W, win->x + win->w + BORDER_W) and g_cursor_y is in [win->y - BORDER_W, win->y + TITLEBAR_H). Alternatively, use win_footprint() logic to compute the full frame extent.
- **[P2 c=0.72 fillable]** focus_window() missing damage update on focus change — `userspace/compositor/compositor_m8.c:5027-5032`
  - focus_window() calls z_push_front() which does mark_dirty() (line 1366), but the function does not ensure damage includes both the OLD focused window's titlebar and the NEW focused window's titlebar. When focus changes, two window titlebars change color (unfocused -> focused), but only z_push_front's generic 'repaint' flag is set without specific damage rectangles. With damage scissoring enabled, 
  - *fix:* After z_push_front(), add specific damage_add() calls for both the old focused window's footprint and the new focused window's footprint to ensure both titlebar color changes are captured in the damage rect.
- **[P2 c=0.68 fillable]** Right dock magnify animation lack of full recomposite forcing — `userspace/compositor/compositor_m8.c:1674-1682`
  - When a right dock icon is hover-magnified (scale easing), the code correctly forces full recomposites (g_full_damage_cooldown = FULL_DAMAGE_COOLDOWN_FRAMES at line 1682). However, this is only set when the icon is CURRENTLY easing. If an icon's scale_target is reached and scale_q8 becomes equal to scale_target, the cooldown expires and future frames use damage scissoring. If a window commits damag
  - *fix:* Track the previous frame's g_rdock_hovered value separately from the render-time hover detection, and when hover STATE changes (entered/exited), force full recomposites independently of animation state, similar to desktop hover tracking at lines 5349-5352.
- **[P3 c=0.65 fillable]** Window drag position updates lack explicit damage marking — `userspace/compositor/compositor_m8.c:5445-5450`
  - When dragging a window (dw->x and dw->y updated at 5445-5446), there is no explicit mark_dirty() call in the drag code path. The window move is committed but damage is not explicitly flagged. The code relies on the hover detection at line 4917 (g_buttons != 0 triggers mark_dirty) to cause recomposite, which is indirect and fragile. If the hover logic changes, window drag could become invisible.
  - *fix:* Add mark_dirty() immediately after dw->x/dw->y are updated at lines 5445-5446, and/or add damage_add() calls to track the old and new window footprints for efficient scissoring.
- **[P3 c=0.55 fillable]** Clamp window right edge calculation may allow windows to slide under dock — `userspace/compositor/compositor_m8.c:4103`
  - The clamp_window() max_x calculation at line 4103 is: (int32_t)g_fb_w - RDOCK_W - (int32_t)win->w - 4. If g_fb_w or RDOCK_W are 0 (uninitialized), or if win->w is very large, this could compute a negative max_x. Although line 4107 checks max_x >= 4, a large window could compute max_x as negative, fail the check, and x remains unclamped. Additionally, subtracting win->w from g_fb_w could produce va
  - *fix:* Clamp max_x to at least 4 explicitly before the conditional: max_x = (int32_t)g_fb_w - RDOCK_W - (int32_t)win->w - 4; if (max_x < 4) max_x = 4; Or compute it as: int32_t avail = (int32_t)g_fb_w - RDOCK_W - 4 - 4; int32_t max_x = (int32_t)win->w < avail ? avail - (int32_t)win->w : 4;
